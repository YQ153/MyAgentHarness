# 环境变量：含 API Key，绝不能入库
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
# 测试覆盖率产物：每次跑 pytest 都会重新生成，入库只会制造噪音
.coverage

# 虚拟环境
.venv/
venv/

# 运行时数据：检查点数据库与会话状态
.data/

# 工作区：保留记忆模板与技能目录，忽略其余运行产物
workspace/*
!workspace/AGENTS.md
!workspace/skills/

# IDE：各开发者环境配置不同，入库只会产生无意义冲突
.idea/
*.iml
