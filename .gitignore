# 环境变量：含 API Key，绝不能入库
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
# 测试与静态检查的缓存/产物：每次运行都会重新生成，入库只会制造噪音
.coverage
coverage.xml
.pytest_cache/
.import_linter_cache/

# 虚拟环境
.venv/
venv/

# 运行时数据：检查点数据库与会话状态
.data/

# 工作区：保留记忆模板与技能目录，忽略其余运行产物
workspace/*
!workspace/AGENTS.md
!workspace/skills/
# 内置技能随仓库交付，必须入库；否则新克隆的机器上「内置技能」这个功能不存在
!workspace/skills-builtin/

# IDE：各开发者环境配置不同，入库只会产生无意义冲突
.idea/
*.iml
