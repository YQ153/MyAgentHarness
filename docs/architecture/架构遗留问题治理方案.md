# MyAgentHarness 架构遗留问题治理方案

> 版本：v1.0
> 日期：2026-09-20
> 评估范围：全项目 Python 源码（230 个 `.py`）的模块边界、规则重复实现与文档一致性
> 前序文档：`分层架构评估与重构方案.md`（2026-09-16 方案）、`分层架构重构实施记录.md`、
> `运行服务模块拆分实施记录.md`
> 性质：**方案**（尚未实施）。实施后另立《架构遗留问题治理实施记录》，本文保留为决策依据。
>
> 本文只收录**当前仍然成立**的问题。前序方案的 P1–P7 已全部闭环（见第 2 节实测核对），
> 照抄前序文档会拿到已经不存在的代码与已经不成立的结论。
>
> **第二批次（2026-09-20 追加）**：第 3 节新增 Q8–Q11，第 4 节新增方案八–十一，
> 议题范围是 `application` 层的**依赖纯度与层内约束**。Q1–Q7 的结论与实施状态不受影响；
> 本批次**不重启已取消的 Q4**——不做任何文件拆分，也不给文件头补"超 500 行"的理由。
>
> **第二批次已实施（2026-09-20）**：四条方案全部落地，实测结果见 5.1 节。
> 未另立《架构遗留问题治理实施记录》——两个批次的状态与实测都回填在本文档内
> （与 Q1–Q7 的处理方式一致）；另立一份只会让"当前状态"散在两处。

---

## 目录

1. [背景与目的](#1-背景与目的)
2. [现状核对：前序问题已闭环](#2-现状核对前序问题已闭环)
3. [问题清单](#3-问题清单)（Q1–Q7；第二批次 Q8–Q11）
4. [治理方案](#4-治理方案)（方案一–七；第二批次 方案八–十一）
5. [实施顺序与依赖](#5-实施顺序与依赖)
6. [总验收标准](#6-总验收标准)
7. [风险与缓解](#7-风险与缓解)
8. [附录](#8-附录)

---

## 1. 背景与目的

### 1.1 目的

前序重构（2026-09-16 起）解决了层间边界问题。本次评估要回答三个新问题：

1. 前序重构之后，还剩哪些架构问题？
2. 每个问题由哪几行代码举证？
3. 按什么顺序修、拿什么验收？

### 1.2 评估依据

沿用前序文档的三条原则，并补充两条——前三条描述"边界在哪"，后两条描述"边界之外还有什么会腐烂"：

| 原则 | 判定标准 |
| --- | --- |
| **关注点分离** | 每一层只处理一类关注点 |
| **职责单一** | 单个模块的变更理由应当唯一；单文件超过 500 行需给出理由 |
| **依赖方向明确** | 依赖单向收敛，上层可依赖下层，下层不得反向依赖 |
| **规则单一来源** | 同一条业务规则只允许一份实现。允许"委托"（转发调用），不允许"复制"（重写判断） |
| **约定可执行** | 每条架构约定都应有对应契约或测试。**无法被拦截的约定视为不存在**——第五条的引入，正是本次 Q1 的起因 |

### 1.3 量化基线（2026-09-20 实测）

| 指标 | 实测值 | 测量方式 |
| --- | --- | --- |
| 源码 `.py` 文件数 | 230 | 遍历仓库，排除 `.data/`、`.venv/`、`__pycache__/`（方案三后复测） |
| import-linter 契约数 | 6 | `.importlinter` 中 `[importlinter:contract:*]` 段数 |
| 违反契约的反向依赖 | **0 处** | `uv run lint-imports` 全绿 + 逐包检索 `from <上层>` |
| 包级循环依赖 | **0 个** | 同上 |
| `interfaces` 直接导入 `runtime` | **0 处** | `interfaces/` 内检索 `from runtime` / `import runtime` |

### 1.4 对前序一处结论的更正

前一版评估把 `knowledge_runtime.py` 描述为"第二个组装点"。逐行核对后，更准确的说法是：

它是**惰性的进程级服务句柄**，而装配与拆除的**所有权仍在 `bootstrap`**
（`bootstrap/core.py:88` 建起来、`:180-183` 放进 `finally` 拆掉）。所以"两个组装点"
这个说法不成立。

真正成立的问题是：**它不在契约的可校验范围内**（`.importlinter:15-21` 的 `root_packages`
只列了 6 个包），因此"谁能依赖它、它能依赖谁"没有任何约束——这才是方案一要解决的。

> 更正记录保留在此，理由与本文档其余部分一致：把误判写下来，比删掉它更能防止重犯。
> 前序文档 `分层架构评估与重构方案.md` 的历史附注也是同一个处理方式。

---

## 2. 现状核对：前序问题已闭环

本节的作用是**否证**，防止后续读者按前序文档去修改已经不存在的代码。

| 前序编号 | 前序结论 | 当前实测 | 证据 |
| --- | --- | --- | --- |
| P1 | `interfaces` 跨层依赖 `runtime`，13 处 | **0 处** | `.importlinter:27-37` 契约 1 生效；`interfaces/cli.py:19`、`interfaces/web/app.py:17-22` 改为依赖 `bootstrap` |
| P2 | `agent` ↔ `llm` 包级循环 | **已解除** | `.importlinter:42-48` 契约 2；`llm/` 内检索 `from agent` 0 命中 |
| P3 | 装配逻辑 2 套 | **1 套** | `bootstrap/core.py:1-6` docstring 声明单点；`interfaces/cli.py:257`、`interfaces/web/app.py:48` 均调用 `build_app_context` |
| P4 | `web/app.py` 越过应用层依赖 `agent` | **已消除** | `app.py` 不再直接 `from agent.graph`，改由 `bootstrap` 提供已装配对象 |
| P5 | `interfaces/web/auth.py` 36KB，6 类关注点 | **已拆** | 现为 `interfaces/web/auth/` 子包，8 个模块（`deps` / `identity` / `apikey` / `audit` / `audit_api` / `utils` / `router` / `__init__`） |
| P6 | `application/rate_limiter.py` 归属错误 | **已下沉** | 现为 `runtime/rate_limiter.py` |
| P7 | `tool/` 空目录与 `test.txt` | **已删除** | 根目录不再存在 |

**结论**：问题已从"层间边界"转移到"重复实现、契约覆盖盲区、单文件规模、文档漂移"四类。
下面的 Q1–Q7 是本次的全部议题（**Q7 是实施方案六时发现的**——同一类问题，只是长在另一份文档上）。

---

## 3. 问题清单

### Q1：架构契约存在覆盖盲区，根级模块是"法外之地"

**现象**：`.importlinter` 的 `root_packages` 只列了 6 个包，根目录 8 个模块全部在可校验范围之外。

**证据**：

```15:21:.importlinter
root_packages =
    agent
    application
    bootstrap
    interfaces
    llm
    runtime
```

根目录实际存在：`config.py`、`main.py`、`knowledge_runtime.py`、`knowledge_tools.py`、
`text_utils.py`、`thread_utils.py`、`web_safety.py`、`web_tools.py`。

其中 `knowledge_runtime.py` 的依赖面最宽——同时 import 应用层、模型层与存储层：

```30:32:knowledge_runtime.py
from application.knowledge_service import KnowledgeService
from llm.embeddings import build_embeddings
from runtime.knowledge_store import open_knowledge_store
```

**根因**：`root_packages` 是按"目录即包"的思路写的，而根级模块恰恰不是包。
于是这些模块的依赖方向既不进依赖图，也不被任何契约约束。

**代价（已真实发生，不是推测）**：`scripts/inspect_thread.py:90-137` 为了不因导入
`knowledge_runtime` 而连带拉起 `application → agent.graph`，被迫**复制**一份
`KNOWLEDGE_DB_NAME` 常量并写导入失败的兜底路径。这是契约盲区逼出来的重复。

**第二处盲区（动态加载）**：自定义工具是**按字符串**加载的（`CUSTOM_TOOL_MODULES`），
`agent/tooling.py:79` 通过 `load_custom_tool_modules` 导入模块名，静态分析看不到这条边。
`tests/test_web_tools.py:24`、`tests/test_knowledge_tools.py:24` 直接
`from web_tools import ...` / `import knowledge_runtime`——这些模块的依赖同样不受约束。

**影响**：

- 契约承诺的"层间不可越界"只对 6 个包成立，根级模块可以任意依赖任何东西
- 新增一个根级模块时，没有任何机制提醒作者"这里也需要约束"
- 插件模块（`web_tools` / `knowledge_tools`）在运行期被加载进 Agent，其依赖面决定了
  一次提示词注入能碰到多少内部实现——目前完全不受控

---

### Q2：同一条规则存在多份实现，且已经漂移

**现象**：两条规则各有"权威实现 + 复制品"，且复制品已经与权威实现产生可观测差异。

**证据 1 —— 会话 ID 校验规则被抄了一遍**：

```324:333:runtime/usage_store.py
    @staticmethod
    def _validate_thread_id(thread_id: str) -> str:
        if not isinstance(thread_id, str):
            raise ValueError(f"thread_id 必须是字符串，实际：{type(thread_id).__name__}")
        normalized = thread_id.strip()
        if not normalized:
            raise ValueError("thread_id 不能为空")
        if len(normalized) > 128:
            raise ValueError("thread_id 过长")
        return normalized
```

对照权威实现 `thread_utils.py:25-44`：

| 项 | `thread_utils.normalize_thread_id`（权威） | `usage_store._validate_thread_id`（复制品） |
| --- | --- | --- |
| 长度上限来源 | 常量 `MAX_THREAD_ID_CHARS`（`thread_utils.py:17`） | 字面量 `128` 硬编码 |
| 超长报错 | `thread_id 过长（{len} > {MAX}）` | `thread_id 过长`（不含长度） |
| 位置 | 中立模块，三层共用 | `UsageStore` 静态方法 |

同一个包内的另外两处都是**委托**而非复制：

```339:346:runtime/thread_store.py
    @staticmethod
    def _validate_thread_id(thread_id: str) -> str:
        """校验会话 ID 并返回规范化结果。

        Raises:
            ValueError: 为空、非字符串或超出长度上限。
        """
        return normalize_thread_id(thread_id)
```

`runtime/attachments.py:95-110` 同样委托。**同一个 `runtime` 包里，"委托"与"复制"
两种做法并存**——这正是规则即将漂移的标准形态：改 `MAX_THREAD_ID_CHARS` 时，
`usage_store` 不会跟着变，而失效方式是"接口放行、用量入库被拒"这类只在特定
输入长度下才暴露的错误。

**证据 2 —— 截断实现三份，用户可见文案各不相同**：

| 位置 | 签名 | 截断提示 |
| --- | --- | --- |
| `text_utils.py:36`（权威） | `truncate(text, limit)` | `…`（省略号占一个字符） |
| `web_tools.py:102` | `_truncate(text, limit)` | `\n\n... Output truncated at {limit} chars.` |
| `knowledge_tools.py:55` | `_truncate(text)`（用模块常量） | `…（片段已截断）` |

三者的**截断语义**（保留多少字符、是否把提示算进上限）与**提示语**都不同。
差异本身可以是有意的（面向模型的工具输出与面向界面的标题，阈值与措辞本就该分开），
但**判断逻辑**没有必要各写一份。

---

### Q3：两个入口的认证路径没有收敛（安全相关）

**现象**：装配已经收敛到单点，但认证语义没有。

`bootstrap/core.py:3-5` 写下收敛装配的理由，原文点名了要避免的风险：

```3:5:bootstrap/core.py
WHY 收敛为单点：装配逻辑一旦出现两份副本，新增一个 store 参数就必须同步
修改两处，任何一处遗漏都会造成「Web 有审计、CLI 无审计」这类安全盲区，
而这类差异难以通过功能测试发现。
```

**而认证路径正是这样的两份副本**：

| 步骤 | CLI（`interfaces/cli.py:149-176`） | Web（`interfaces/web/auth/deps.py:77-143`） |
| --- | --- | --- |
| dev key 比对 | `secrets.compare_digest`（`:159`） | `secrets.compare_digest`（`:88`） |
| 查存储 | `api_key_store.validate`（`:167`） | `store.validate`（`:116`） |
| 构造主体 | 构造 `Principal`（`:169-175`） | 构造 `Principal`（`:137-143`） |
| **成功写审计** | **无** | `log_auth_event`（`:89-96`、`:129-136`） |
| **失败写审计** | **无** | `log_auth_event`（`:106-113`、`:118-125`） |

即：**同一个认证动作，Web 端落 4 类审计事件，CLI 端一条都不落**。这恰好是
`bootstrap/core.py` 想要避免的那类分叉，只是漏在了认证路径上——因为认证逻辑
不在 `bootstrap` 装配的对象里，而是以模块函数形式存在。

**次要差异**：同一个 dev key 构造出的 `Principal` 字段不同——
`deps.py:97-103` 显式带 `scopes=frozenset(ROLE_PERMISSIONS["admin"])`，
`cli.py:160-165` 不带（落到 `principal.py:37` 的默认空集）。
当前 `Principal.has_permission`（`principal.py:40-49`）只读 `role`，所以尚未造成越权；
但两个入口对"同一个主体是谁"的描述已经不一致，日后若让 `has_permission` 参考
`scopes`，CLI 侧会静默失去权限。

**影响**：安全事件的可追溯性在两个入口不对等；换 key 校验实现（加缓存、改哈希算法、
加撤销检查）必须改两处，漏一处即产生一个入口的静默降级。

**修复状态（2026-09-20）**：已由方案三修复——校验收敛为 `application/api_key_auth.py`
一份实现，CLI 补齐成功/失败审计（带 `entry="cli"`），dev key 的 `Principal.scopes`
两端对齐。两处行为变更与一处顺手修掉的缺陷（非 ASCII 凭据把 401 变成 500）见该节。

---

### Q4：单文件规模超出项目自定标准

**现象**：项目自定标准是"单文件超过 500 行需给出理由"（`分层架构评估与重构方案.md:64`）。

**实测（2026-09-20）**：

| 文件 | 行数 | 实际承载 | 是否已有理由 |
| --- | --- | --- | --- |
| `interfaces/web/static/app.js` | **2409** | 全部前端逻辑，无模块划分 | 无 |
| `application/run_service.py` | **1342** | 运行推进 + 事件流；docstring 声称已拆四块，本体仍是全仓最大 `.py` | 部分（拆分实施记录） |
| `config.py` | **1248** | 配置中心 + 沙箱白名单 + MIME 默认值 + 暴露告警 + 目录准备 | 部分（常量下沉有 WHY） |
| `runtime/thread_store.py` | **1083** | 单表存储 + 标题规范化 + 搜索 + 分页 | 无 |
| `application/thread_service.py` | **1069** | 会话生命周期（清单/历史/导出/删除） | 无 |
| `runtime/knowledge_store.py` | **900** | FTS5 + vec0 双索引 | 无 |
| `interfaces/web/routes.py` | **862** | REST 端点（19 个，其中 4 处以 SSE 返回）+ DI 辅助 + SSE 编码 | 无 |

> 行数为 2026-09-20（方案四取消时）重测；此前表里的 `thread_store` / `knowledge_store`
> 行数与 `routes.py` 的端点数量（旧记 21 个）已随重构变化，一并订正。

**修复状态（2026-09-20）：已取消**——用户决定不做本轮拆分，Q4 转为**已知限制**。
已实测确认：CI（`.github/workflows/ci.yml`）只跑 pytest 与 `lint-imports`，
**没有任何行数检查**，所以"单文件超 500 行需给出理由"这一条**完全依赖人工**；
取消后，这几个文件的规模只增不减时不会有任何东西提醒。
判据与逐文件处理方式保留在下面的方案四。

**注意**：`config.py` 中把沙箱白名单等常量留在配置层**是有正当理由的**
（`config.py:46-47` 说明：放 `runtime` 会让 `config` 反向依赖 `runtime`）。
Q4 不否认该权衡，只是指出**行数这一项仍未达标**，且理由应当写在文件里。

**另一处不一致 —— 层的对外声明与事实不符**：

```1:8:application/__init__.py
"""应用层：把 LangGraph 的内部事件翻译成稳定的对外协议。

对外提供三件事，彼此职责互不重叠：

- :class:`ThreadService` 会话的元数据生命周期（清单、历史、删除）
- :class:`RunService`   一次运行的推进与事件产出
- :class:`ModelCatalog` 可切换模型的只读目录
"""
```

而 `application/` 实际有 **29 个模块**（附件、知识库、记忆、技能、用量、工作区、
健康、分叉、治理、运行登记……），`__all__`（`:24-37`）只导出 12 个符号。
新读者按 `__init__.py` 理解这一层，会漏掉 20 多个模块。

---

### Q5：`runtime` 的 7 个 `open_*` 生命周期样板逐字重复

**现象**：7 个存储模块各自实现一遍相同的连接生命周期。

**清单**：`open_thread_store`（`thread_store.py:1046`）、`open_audit_store`（`audit_store.py:307`）、
`open_api_key_store`（`api_key_store.py:272`）、`open_usage_store`（`usage_store.py:367`）、
`open_skill_store`（`skill_store.py:300`）、`open_knowledge_store`（`knowledge_store.py:773`）、
`open_store`（`store.py:25`）。

**逐字重复的结构**：

| 结构 | 出现位置 |
| --- | --- |
| `db_path.parent.mkdir(parents=True, exist_ok=True)` + `ValueError("db_path 不能为 None")` | 全部 7 个 |
| `PRAGMA journal_mode=WAL;` + `PRAGMA busy_timeout=5000;` | `thread_store.py:1081-1084`、`store.py:62-63`、`knowledge_store.py:818-819` |
| `try/except Exception: logger.exception("<X>初始化失败"); raise` | `thread_store.py:1094-1096`、`knowledge_store.py:870-872`、`store.py:69-71` |
| `finally: await conn.close(); logger.info("<X>连接已关闭")` | `thread_store.py:1100-1103`、`store.py:74-78` |
| 每个 store 类各持一把 `asyncio.Lock` | `api_key_store.py:94`、`skill_store.py:79`、`thread_store.py:335` |

**为什么这不是"可以接受的重复"**：项目自己已经确认它是风险点，并为此写了一个专门的
测试文件：

```3:6:tests/runtime/test_open_store_contract.py
WHY 单独成文件：这条契约由 runtime 下六个模块**共同**承担——``open_thread_store`` /
``open_audit_store`` / ``open_api_key_store`` / ``open_usage_store`` /
``open_skill_store`` / ``open_knowledge_store``。它们各自的业务测试都在自己文件里，
而出问题的是六份实现里**同一处**结构写法，逐文件各写一条只能证明写了六遍。
```

> 引文是**修复前**的措辞。方案五把实现收敛成一份之后，该 docstring 已改写为
> "那份实现的行为规格"——同一句"逐文件各写一条只能证明写了六遍"在只剩一份实现时
> 不再成立，留着它就成了一份理由已失效的文档。

该文件的 docstring 还记录了真实代价：`python main.py cli` 在 `auth_mode=apikey` 且未设
`HARNESS_API_KEY` 时，真正原因只有一句话，日志里却是 5 条"XX 初始化失败"加 5 份重复堆栈，
排查方向被引向数据库。

**既然已经确认是"同一处"，用 6 份实现去满足 1 条契约就不是终点**。

**附带问题**：store 类**自身没有 `open` / `close`**，生命周期只存在于模块级函数里
（检索 `def open_|def close|async def initialize` 在 `*_store.py` 的类中 0 命中）。
后果是 store 的交易行为可以被单测，但"连接是否正确建立与释放"只能通过模块函数测。

**修复状态（2026-09-20）**：已由方案五修复——生命周期收敛为 `runtime/sqlite_lifecycle.py`
的一份实现，7 个入口改为声明"建哪张表、产出什么对象、日志叫什么"。
上面那条"附带问题"同时被消解：连接是否正确建立与释放，现在由契约测试对
**全部 7 个入口**逐个验证（此前它只覆盖 6 个，见方案五的现状核查）。

---

### Q6：README 的目录结构与分层描述已与代码脱钩

**现象**：README 是唯一的对外入口文档，其目录树缺一个完整的顶层包。

```540:554:README.md
MyAgentHarness/
├── main.py             # 入口：分发 cli / web 两个子命令
├── config.py           # 统一配置中心（唯一的环境变量解析处）
├── agent/              # 装配层：模型后端、工具、护栏、图构建
├── application/        # 应用服务：会话编排、事件模型、中断编解码
├── interfaces/         # 适配器
│   ├── cli.py          # 命令行交互
│   └── web/            # FastAPI 服务 + 前端页面
├── llm/                # 模型注册表与构造
├── runtime/            # 检查点持久化、会话元数据表与长期存储
└── workspace/          # Agent 的活动边界（文件读写、记忆、技能）

分层约定：`interfaces` → `application` → `agent` / `runtime`，上层依赖下层，下层不反向依赖。
```

**问题 1 —— 缺项**：目录树没有 `bootstrap/`。全仓检索确认，README 中"bootstrap"
出现 **0 次**。同时缺 `scripts/`、`tests/`，以及 `thread_utils.py`、`knowledge_runtime.py`、
`web_tools.py`、`web_safety.py`、`knowledge_tools.py` 五个根级模块。

**问题 2 —— 分层描述与契约冲突**（比缺项更严重）：

| README 第 554 行 | `.importlinter` 契约 1 |
| --- | --- |
| `interfaces` → `application` → `agent` / `runtime`，暗示 `interfaces` 可经 `application` 到达 `runtime` | `interfaces` **不得直接依赖** `runtime`（`:27-37`） |
| 未提及 `interfaces → bootstrap` | 实际存在：`interfaces/cli.py:19`、`interfaces/web/app.py:17` |

照 README 的字面理解写代码，会写出被 CI 拦下的导入。**文档与契约必须一致，
否则契约就从"团队共识"退化成"CI 才知道的规则"。**

**修复状态（2026-09-20）**：已由方案六修复；同一轮还修掉了 `architecture.html` 的同类问题（见 Q7）。

---

### Q7：`architecture.html` 的行号引用与代码脱钩，而页脚承诺它们一致

**现象**：`architecture.html` 被其它文档指定为"当前结构以此为准"
（`分层架构重构实施记录.md:11`），但它的第 3 章目录树与行号引用都已滞后。

**证据 1 —— 目录树是三份文档里最旧的一份**：

| 项 | 该树的写法 | 实际 |
| --- | --- | --- |
| 组装层 | **完全没有 `bootstrap/`** | `bootstrap/` 共 4 个模块，是全应用唯一的装配点 |
| 根级模块 | 只有 `main.py` / `config.py` / `text_utils.py` / `thread_utils.py` | 另有 `web_safety.py` / `web_tools.py` / `knowledge_runtime.py` / `knowledge_tools.py` |
| `tool/` | 标注为"历史遗留空目录（早期文件工具原型已移除）" | **已在 P7 删除**，照它找目录会找不到 |
| `application/` | 列 15 个模块 | 实际 29 个 |
| `runtime/` | 列 4 个模块 | 实际 28 个模块（含 `sandbox/` 子包 9 个） |

**证据 2 —— 行号引用（实测三例）**：

| 表中标注 | 实测 | 偏移 |
| --- | --- | --- |
| `main() main.py:79` | `main.py:137` | +58 |
| `open_thread_store() :360` | `runtime/thread_store.py:1046` | +686 |
| `ThreadMetaStore :76` | `runtime/thread_store.py:321` | +245 |

偏移量不一致，说明其间不是一次整体平移，而是多次结构性改动叠加——
**因此不能按常量偏移批量修正，必须逐符号取证**。

**根因**：该文档的维护方式是"逐条补注"（页脚有明示）。补注保住了**结论**，
却没管**引用坐标**；而页脚同时承诺"所有位置引用格式为 `文件:行号`，与源文件一致"——
承诺与维护方式自相矛盾。

**影响**：读者按行号跳转会落到无关代码上；更严重的是**那句承诺本身让人不再怀疑**，
于是无人复核。

**修复状态（2026-09-20）**：目录树已由方案六重生成；全篇引用已由方案七逐条复核完毕
（84 处里只有 3 处仍然正确，现已全部修正并补成带路径形式），页脚承诺也据此改写为
"引用由 `tests/test_architecture_doc_contract.py` 持续守住"。
复核还订正了 4 处**内容**已过时的断言（不是行号漂移），其中两处（`interfaces` 直接依赖
`runtime`、"`llm/registry.py` 反向导入 `agent.profiles`"）读起来像"被接受的架构例外"——
详见方案七的复核结论。

---

### Q8：应用层的端口隔离只覆盖了接口层那一侧

**现象**：`application/ports.py` 的三个 Protocol（`AuditSink` / `APIKeyRepository` / `RateLimiterPort`）
全部为 `interfaces` 服务，`application` **自身**对 `runtime` 实现类的依赖不在任何约束之内。

**证据 1 —— `ports.py` 的设计动机只写了接口层**：

```3:11:application/ports.py
WHY 需要端口：``interfaces`` 层需要操作审计日志、API Key 与 device flow 状态，
但不应依赖 ``runtime`` 的具体实现类——那会让「更换存储实现」波及接口层，
也破坏 ``interfaces → application`` 的单向依赖。

用 ``typing.Protocol`` 描述所需能力后：

- ``interfaces`` 只依赖 ``application``，依赖方向合规；
- ``runtime`` 的实现类因结构化子类型（structural subtyping）自动满足协议，
  无需显式继承，也无需反向导入本模块，因此不产生新的耦合。
```

**证据 2 —— 同一件事在应用层内部没有做**（2026-09-20 实测，逐条检索导入点）：

| 使用方 | 导入的 `runtime` 实现符号 | 性质 |
| --- | --- | --- |
| `application/thread_service.py:43,48,49` | `ThreadMetaStore`、`AuditStore`、`attachments` 函数组 | 实现类 + 函数 |
| `application/run_service.py:66-71` | `AuditStore`、`ThreadMetaStore`、`UsageStore`、`tool_outputs` 函数组、`workspace_files.to_virtual_path` | 实现类 + 函数 |
| `application/usage_service.py:19` | `UsageStore`（TYPE_CHECKING）、`window_start` | 实现类 + 函数 |
| `application/knowledge_service.py:27-29` | `KnowledgeStore`、`ChunkInput`、`KnowledgeHit`、`workspace_files` 函数组、`llm.EmbeddingError` | 实现类 + 数据载体 + 函数 |
| `application/skill_service.py:27-29` | `SkillStateStore`、`skill_view` 函数组、`skills.inspect_skills` | 实现类 + 函数 |
| `application/attachment_service.py:27` | `attachments` 函数组 | 函数 |
| `application/run_registry.py:30,203` | `RateLimiter`（由登记表**自己构造**） | 实现类 |
| `application/run_governance.py:21` | `execution_registry.abort_scope` | 上下文管理器 |

**根因**：端口化此前被理解成"让 `interfaces` 不依赖 `runtime`"这一条**具体诉求**，
而不是"应用层不依赖基础设施实现"这条**通用规则**。于是接口层修干净了（Q1 的实测：
`interfaces` 直接导入 `runtime` **0 处**），应用层原样保留。契约 3
（`runtime` 是叶子）只禁止 `runtime` 反向依赖，不禁止 `application` 依赖具体实现，
因此没有任何东西会拦。

**代价**：更换存储实现（SQLite → 其它）时，`bootstrap` 换掉实例并不够——
`application` 的构造注解与 import 也要跟着改。这正是 `ports.py` 想要消除的那类波及面，
只是没覆盖到应用层自己。

**修复状态（2026-09-20）**：已由方案八修复——`application` 内对 `runtime` 实现类的
导入点降为 **0**；白名单之外的直接依赖由新增契约拦住（无状态函数、数据载体与
`RateLimiter` 按判据表登记为有意保留）。

---

### Q9：`application` 依赖 `agent` 的内部实现细节

**现象**：依赖方向合规（`application → agent` 是 README 与契约都允许的），
但被依赖的三个符号里，有属于**内核装配细节**的：

| 依赖 | 位置 | 性质 |
| --- | --- | --- |
| `AgentRunContext` | `application/run_service.py:31` | 图的**入参上下文** |
| `ANONYMOUS_USER_ID` | `application/principal.py:17`、`run_registry.py:23`、`memory_service.py:17` | 记忆命名空间的**兜底标识** |
| `BUILTIN_TOOL_NAMES` | `application/tool_catalog.py:16` | `agent.tools` 的内置工具名表 |

**证据 —— 第三项是"应用层回查内核常量"**：

```72:75:application/tool_catalog.py
        items: list[ToolInfo] = [
            ToolInfo(name=name, source="builtin") for name in self._builtin_names()
        ]
```

`_builtin_names()`（`tool_catalog.py:126-132`）与 `source_of()`（`:112-124`）
都直接读 `BUILTIN_TOOL_NAMES`，而 `ToolBundle`（`agent/tooling.py:30-54`）
**本来就已经**是装配结果的载体（含 `descriptors` / `custom_modules` / `mcp_statuses`），
只是"哪些是内置"没有随它一起交出来。

**注意**：本问题**不主张**把三条依赖全解掉。逐条评估见方案九：
其中两条经评估判定"不改"，理由必须留在文档里——否则下一个读到这里的人会把它们重新解一遍。

**修复状态（2026-09-20）**：已由方案九处理——`BUILTIN_TOOL_NAMES` 改由
`ToolBundle.builtin_names` 携带（应用层实测 0 处直接依赖）；`AgentRunContext` 与
`ANONYMOUS_USER_ID` 经评估**有意保留**，理由见方案九的评估表。

---

### Q10：`application/__init__.py` 的层声明与事实不符

**现象**：该文件的 docstring 把整个应用层描述成"对外提供三件事"，而层内实际有 **30 个模块**、
九类职责域。

```1:8:application/__init__.py
"""应用层：把 LangGraph 的内部事件翻译成稳定的对外协议。

对外提供三件事，彼此职责互不重叠：

- :class:`ThreadService` 会话的元数据生命周期（清单、历史、删除）
- :class:`RunService`   一次运行的推进与事件产出
- :class:`ModelCatalog` 可切换模型的只读目录
"""
```

`__all__`（`:24-37`）只导出 12 个符号。新读者按这段 docstring 理解这一层，
会漏掉附件、知识库、记忆、技能、用量、工作区、健康、分叉、治理、运行登记等 20 余个模块。

**来源**：该修正属于方案四步骤 5，随方案四整体取消（2026-09-20 用户决定）而未落地，
因此它是**遗留项**而不是新问题。本次不重启方案四的拆分部分，只做这一处文档修正。

**修复状态（2026-09-20）**：已由方案十修复。docstring 逐一点名 29 个模块，
并由 `test_layer_purity_contract.py` 的两条断言持续守住（点名不全、
`__all__` 指向不存在的符号都会失败）。

---

### Q11：包内依赖方向是契约盲区

**现象**：`.importlinter` 的 6 条契约**全部**作用在包与包之间（`root_packages` 是 6 个包），
包**内部**平铺模块之间"谁可以依赖谁"没有任何约束。当前 `application/` 是单层平铺的 30 个模块。

**实测（2026-09-20）**：当前**无环、无逆流**——契约组模块
（`dto` / `errors` / `events` / `ports` / `principal` / `audit_recorder` / `ownership`）
不依赖任何服务模块。但这一点没有任何机制会持续保证。

**根因**：与 Q1 的形态同构——**契约管不到的地方会腐烂**，只是这次的盲区从"根级模块"
换成了"包内模块"。差别在于 Q1 已被 `tests/test_root_module_contract.py` 补上，
包内这一层仍是空白。

**失败形态（可预期）**：某天 `application/dto.py` 为了返回一个 `AttachmentInfo`
而 `from application.attachment_service import ...`，包内环就此形成，
而 `lint-imports` 与全量 pytest 全绿——与被 Q1 记录的那类问题完全一样。

**注意**：本条**无实际违规证据**，属预防性规则，故在方案十一中标注为"可选加固"。

**修复状态（2026-09-20）**：已由方案十一补上（实测实施前 0 违规，契约落地即绿），
并顺带把根级契约测试的 AST 解析收敛到 `tests/_ast_imports.py`，避免第二份实现。

---

## 4. 治理方案

### 方案一：把根级模块纳入契约可校验范围（对应 Q1）

> **实施状态：已实施（2026-09-20）。承载机制与初版设计不同，原因见下节「工具限制（实测）」。**

#### 目标

让根级模块的依赖边界变成**可执行、CI 会拦**的规则，使规则覆盖面从
"6 个包不可越界"扩展为"**全仓无盲区**"。

目标与初版设计一致，改变的只是承载机制——初版打算用 `.importlinter`，实测走不通。

#### 工具限制（实测，决定了机制的选择）

初版方案要把 8 个根级模块写进 `.importlinter` 的 `root_packages` 并新增 5 条契约。
**这条路走不通**，两次尝试各撞一个硬约束（import-linter 2.15 / grimp 3.17 实测）：

| 尝试 | 实际报错 |
| --- | --- |
| 把 `config` 等写进 `root_packages` | `'config' is a module, not a package. root_packages should only contain packages (directories with __init__.py), not individual .py files.` |
| 退一步：`root_packages` 保持 6 个包，改在契约里引用 `config` | `Module 'config' does not exist.` |

即 import-linter **只接受"包"作为分析根**：单文件模块既不能列为分析根，也不能被契约引用。
要让它看见这些模块，唯一办法是把它们移进某个目录包，而这不可接受：

- `config` 被全仓数十处 `from config import ...` 引用，改动是全局的；
- `knowledge_tools` / `web_tools` 的名字是**配置项取值**（`CUSTOM_TOOL_MODULES=web_tools`），
  移进包会变成 `CUSTOM_TOOL_MODULES=plugins.web_tools`，属面向使用者的破坏性变更。

**结论：换机制，不换规则。** 同一份角色表落成 `tests/test_root_module_contract.py`，
由 `uv run pytest` 执行——它与 `lint-imports` 跑在 CI 的同一个 job 里
（`.github/workflows/ci.yml` 的 `Run tests with coverage` 与
`Enforce layered-architecture contracts` 两步），因此"可执行"与"CI 会拦"
这两条性质没有损失。

#### 为什么不是"把 `knowledge_runtime` 搬进 `bootstrap/`"

前一版评估提过这个选项，现予否决，理由：

`knowledge_runtime` 存在的**唯一**理由是被自定义工具插件取用
（`knowledge_runtime.py:3-7`：扩展点只把 `config` 交给工具模块，没有注入依赖的通道）。
而 `knowledge_tools.py:22` 正是这样的插件。若把它搬进 `bootstrap/`，就会得到
**插件 → 装配层** 的依赖：一个运行期被动态加载进 Agent 的第三方式模块，
反过来依赖应用的组装点。这比"根级模块不在契约内"更糟——契约 6 的意图
（装配层只能被接口层调用）会被从插件侧绕开。

**保留在根级、但纳入契约**，才是既保住插件可达性、又堵住盲区的做法。

#### 适用范围（实际实施）

| 文件 | 变更 |
| --- | --- |
| `tests/test_root_module_contract.py` | **新增**：角色表 + 4 类断言（含反方向那一类） |
| `.importlinter` | 尾部增加**反向指路注释**（ASCII），说明根级模块为何不在此文件内、去哪找；6 条既有契约零改动 |
| 根级 8 个模块 | **零改动**——本次只让规则可见，不碰代码 |

#### 实施步骤（实际实施）

**步骤 1 —— 角色表**：以 `_BOUNDARIES: dict[str, frozenset[str]]` 声明每个根级模块
允许依赖的仓库内顶层模块。角色划分与初版设计完全相同：

| 角色 | 模块 | 允许的仓库内依赖 | 依据 |
| --- | --- | --- | --- |
| 中立叶子 | `config` / `text_utils` / `thread_utils` / `web_safety` | **空集**，不得依赖仓库内任何模块 | 它们存在的理由就是"最底层、跨层共用"（`text_utils.py:3-6`、`thread_utils.py:3-8`）。一旦任一 import 了上层，下沉的理由消失，且会立刻构成循环 |
| 服务句柄 | `knowledge_runtime` | `application` / `llm` / `runtime` / `config` | 装配与拆除的所有权在 `bootstrap`（`bootstrap/core.py:88`、`:180-183`），取用方是知识库插件（`knowledge_tools.py:22`） |
| 工具插件 | `web_tools` / `knowledge_tools` | `agent`（注册 SPI）、`config`、`web_safety`、`knowledge_runtime` | 插件被动态加载进 Agent 进程，只能依赖注册 SPI、中立模块与自己的服务句柄；直接摸 `application` / `runtime` 会绕过应用层的归属校验、审计与限流，而插件是最暴露于提示词注入的那部分代码 |
| 入口分发 | `main` | `application` / `config` / `interfaces` | `main.py` 只做"解析参数 → 分发"。装配必须经 `interfaces` → `bootstrap`，否则 P1/P3 式的"入口自行装配"会从 `main.py` 长回来 |

**步骤 2 —— 四类断言**。初版设计的 5 条契约全部落到这四类里，一条不丢：

| 初版契约 | 现在由谁承担 |
| --- | --- |
| 7 中立叶子不依赖仓库内任何模块 | 用例 1（`_BOUNDARIES` 里四个空集条目） |
| 8 服务句柄不得被业务层与其它插件**直接**依赖 | **用例 4**（反方向：全量扫描分层包与根级模块，只放行 `bootstrap` 与 `knowledge_tools`） |
| 9 服务句柄自身不直接依赖 `agent` / `interfaces` / `bootstrap` | 用例 1（`knowledge_runtime` 的角色集合里没有这三项） |
| 10 工具插件不直接依赖 `application` / `runtime` / `interfaces` / `bootstrap` / `llm` | 用例 1（两个插件的角色集合里没有这五项） |
| 11 `main` 不直接依赖 `agent` / `runtime` / `llm` / `bootstrap` | 用例 1（`main` 的角色集合里没有这四项） |

四条断言分别是：

1. `test_root_module_imports_stay_within_its_role[...]`（按模块参数化）：实际依赖集合
   必须**等于**角色表声明的集合。
2. 同上用例的第二条断言：角色表里**过时**的条目也必须清理，否则表格会退化成
   "历史记录"，下一个读表的人会以为那条边还在。
3. `test_every_root_module_declares_a_role`：仓库根新增的任何 `.py` 都必须登记角色。
   这条守的是"**盲区不再新增**"——新增根级文件不需要修改任何既有代码，
   不会有任何东西提醒作者它还缺一条约束。
4. `test_service_handle_is_reached_only_by_its_legitimate_callers`：扫描六个分层包与
   八个根级模块，除 `bootstrap` 与 `knowledge_tools` 外不允许任何模块直接依赖服务句柄。

**步骤 3 —— 用 AST 而不是 `sys.modules`**：静态解析与 `.importlinter` 的口径一致，
且覆盖 `if TYPE_CHECKING:` 块与函数内的延迟导入。这不是洁癖：历史上 13 处
`interfaces → runtime` 越界正是靠延迟导入藏住的，而下面的注入验证里有两类正是这种形态。

顺带一个必须澄清的点：**注释中出现的模块名不会被误判**（AST 不含注释节点）。
对本仓库尤其重要——这些模块的 docstring 与注释里大量讨论彼此的依赖关系，
若按文本匹配实现，`interfaces/web/knowledge_routes.py` 与 `interfaces/web/app.py`
里那两句提到 `knowledge_runtime` 的注释会立刻变成误报。

**步骤 4 —— `.importlinter` 留反向指路注释**：写明根级模块为何不在该文件内、去哪找。
没有这条注释，下一个发现"少了一条规则"的人会把模块加进 `root_packages`，
然后撞上前面那两个报错——同一段弯路会被反复探索。

#### 实施后的可校验范围

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| 契约管不到的顶层模块数 | 8 | **0** |
| `.importlinter` 契约数 | 6 条 | 6 条（不变；根级模块由契约测试承担） |
| 插件模块的依赖面 | 无约束 | 由角色表固定 |
| `main.py` 自行装配 | 可发生且无人拦 | 被用例 1 拦住 |
| 除装配层与插件外取用服务句柄 | 可发生且无人拦 | 被用例 4 拦住 |
| 新增根级模块 | 静默无约束 | 必须登记角色，否则 CI 红 |

#### 预期效果（已实测，不是预测）

| 注入的违规 | 实测结果 |
| --- | --- |
| `main.py` 在函数内延迟导入 `from runtime.thread_store import open_thread_store` | `test_root_module_imports_stay_within_its_role[main]` 失败，指出 `main.py 依赖了 ['runtime']` |
| `web_tools.py` 在 `if TYPE_CHECKING:` 块中导入 `application.health` | `test_root_module_imports_stay_within_its_role[web_tools]` 失败，指出 `依赖了 ['application']` |
| 仓库根新建一个无依赖的 `temp_probe_module.py` | `test_every_root_module_declares_a_role` 失败，指出该模块未声明角色 |
| `interfaces/web/routes.py` 直接导入 `knowledge_runtime` | `test_service_handle_is_reached_only_by_its_legitimate_callers` 失败，指出 `interfaces/web/routes.py（属于 interfaces）` |

四处注入均已撤回。撤回后的实测结果：`uv run pytest tests/test_root_module_contract.py`
10 项全过；`uv run lint-imports` 6 条全绿；`uv run pytest` 全量 `1258 passed`。

#### 风险

| 风险 | 概率 | 缓解 |
| --- | --- | --- |
| 机制分散在两处（包用 linter、根级用测试），读者不知道去哪加规则 | 中 | 双向交叉引用：`.importlinter` 尾部指向测试文件，测试文件 docstring 指向 `.importlinter` 与本文档 |
| 角色表被当成"上限"而非"精确集合"，随手放宽 | 中 | 两个方向相反的断言（越界 / 过时）都会失败，放宽必须显式删掉条目；失败信息里写明了这一步的理由要求 |
| AST 解析在语法错误时静默返回空集，让所有断言"通过" | 低 | `_repo_imports` 对读取失败与 `SyntaxError` 都显式 raise；`_root_module_paths` / `_scannable_paths` 扫不到任何文件时同样显式失败。两处 docstring 都写明了「静默失效必须在源头拦住」 |
| 新约束被绕过（改走 `importlib` 加载仓库外模块） | 低 | 动态加载的**目标模块**本身在守备范围内；加载仓库外模块属部署行为，不在静态契约范围 |

#### 验收标准

1. `uv run pytest tests/test_root_module_contract.py` 通过（8 个模块参数化用例 + 2 条单例断言，共 10 项）
2. 注入上述四类违规时该文件报错并指出具体模块与越界依赖；撤回后恢复全绿
3. `uv run lint-imports` 仍为 6 条全绿（本次不改既有契约）
4. `uv run pytest` 全量通过
5. 根级 8 个模块的源码零改动

---

### 方案二：消除规则的两份复制（对应 Q2）

> **实施状态：已实施（2026-09-20）。步骤 2 与初版设计不同**——不是复用 `truncate`，
> 而是新增 `truncate_with_notice`。照初版做会让两处工具输出**改变行为**，见下。

#### 目标

把"复制"改回"委托"，使同一条规则在全仓只有一份判断逻辑。

#### 适用范围（实际实施）

| 文件 | 变更 |
| --- | --- |
| `runtime/usage_store.py` | `_validate_thread_id` 改为委托；新增 `from thread_utils import normalize_thread_id` |
| `text_utils.py` | **新增** `truncate_with_notice`；`truncate` 补 `text` 类型校验 |
| `web_tools.py` | `_truncate` 改为委托（签名与文案逐字不变） |
| `knowledge_tools.py` | `_truncate` 改为委托（签名与文案逐字不变） |
| `tests/test_root_module_contract.py` | 两个插件的角色表补 `text_utils`（见下「契约拦住了这次改动」） |
| `tests/test_text_utils.py` | **新增**：两个截断口径各自的边界、以及「两者不能合并」的断言。该模块此前只被间接覆盖，而间接覆盖发现不了「某一侧的上限悄悄失效」 |

#### 实施步骤

**步骤 1 —— 会话 ID 校验改委托**：方法体改为 `return normalize_thread_id(thread_id)`，
import 照 `thread_store.py:29-30` 的既有写法。保留同名静态方法作为调用点的稳定名字，
不改调用方。

**步骤 2 —— 截断：新增 `truncate_with_notice`，而不是复用 `truncate`**（与初版设计不同）。

初版设计写的是"判断逻辑统一走 `text_utils.truncate`"。**核对后否掉**：两者的长度口径相反——

| | `truncate(text, limit)`（已有） | `truncate_with_notice(text, limit, notice)`（本次新增） |
| --- | --- | --- |
| 面向 | 展示（会话标题） | 工具输出（会被模型当输入读） |
| 省略号 / 提示语 | 省略号**占一个字符** | 提示语**独立追加** |
| 结果总长 | **不超过** `limit` | **可以超过** `limit` |
| 现有调用方 | `build_title` | `web_tools` 抓取正文、`knowledge_tools` 检索片段 |

直接复用 `truncate` 会让两处工具输出悄悄变行为（保留字符数少 1、且提示语样式被换掉），
而这两处各有断言钉着（`tests/test_web_tools.py:443`、`tests/test_knowledge_tools.py:142`）。
**新增一个函数、共用那句 `len(text) <= limit` 判断**，才是"收敛怎么算、不收敛怎么说"。

需要共用的不是口径，而是**判断**：未超限就原样返回，超限才追加提示语。
各写一份最可能出现的偏差是"提示语无条件追加"——于是没被截断的结果也自称被截断了。

**步骤 3 —— 顺带核对硬编码 `128`**：实测 `runtime/` 下与会话 ID 相关的 `128`
只有 `usage_store.py:331` 一处。同文件的 `MAX_MODEL_CHARS`（`:26`）与
`_MAX_OWNER_CHARS`（`:27`）是**另外两条规则**（模型名、所有者 ID 的长度上限），
不在本次范围。

#### 契约拦住了这次改动（方案一的第一次真实回报）

给两个插件加 `from text_utils import ...` 之后，方案一建立的根级模块契约立刻失败：

```
AssertionError: knowledge_tools.py 依赖了 ['text_utils']，
超出它声明的角色范围 ['agent', 'config', 'knowledge_runtime']。
```

这不是误报，而是**要求作者对新增的那条边表态**——正是它存在的目的。评估后放宽：
插件角色的理由是"只能依赖注册 SPI、中立模块与自己的服务句柄"，而 `text_utils`
正是中立叶子（自身零仓库内依赖），与允许集里已有的 `config` / `web_safety` 同类。
于是更新角色表，并在注释里写明这次放宽的理由与日期——**放宽是被记录的动作，
不是被忽略的事实**。

#### 预期效果（实施后实测）

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| 会话 ID 规则的判断实现 | 2 份 | 1 份（`thread_utils`），`usage_store` 为委托 |
| 截断判断逻辑 | 3 份 | 1 份（`text_utils.truncate_with_notice`） |
| 修改会话 ID 上限需要动的文件 | 2 个 | 1 个 |
| 工具输出的提示语 | 两种措辞（各一份） | **两种措辞逐字不变**（有意保留） |

#### 风险

| 风险 | 概率 | 缓解 |
| --- | --- | --- |
| 报错文案改变导致既有测试断言失败 | 中 | 实测未发生：`tests/runtime/test_usage_store.py:40` 断言的是 `match="thread_id"`，新旧文案都含它 |
| 截断提示语被误统一，破坏模型可读性 | 低 | 步骤 2 改为"新增函数"而非"复用"；两处文案逐字保留并由既有用例钉住 |
| 中立模块被当作"什么都能放"的杂物间 | 低 | 契约 7 禁止中立叶子依赖仓库内任何模块，因此它无法变成中转站 |

#### 验收标准（已达成）

1. `runtime/usage_store.py` 中与会话 ID 相关的字面量 `128` 不再出现。
   （注：初版标准写的是"不再出现字面量 `128`"，**口径过宽**——它会要求改掉两条
   无关规则的长度上限；此处收窄为"与会话 ID 相关"）
2. `usage_store._validate_thread_id` 的方法体只有一行 `return normalize_thread_id(thread_id)`，
   报错文案因此与 `thread_utils` 逐字一致
3. `web_tools` / `knowledge_tools` 的截断提示语逐字未变，且 `_truncate` 的签名未变
4. `uv run pytest tests/runtime/test_usage_store.py tests/test_web_tools.py tests/test_knowledge_tools.py` 通过
5. 新增 `tests/test_text_utils.py`（14 项）钉住两个截断口径的边界，并有一条断言明确
   「两者不能合并」——否则后来者最自然的动作就是合并省事
6. `uv run pytest` 全量通过；`uv run lint-imports` 仍为 6 条全绿

---

### 方案三：认证校验收敛为共享实现（对应 Q3）

> **实施状态：已完成（2026-09-20）**。这是本方案里唯一**有意改变行为**的一项
> （CLI 开始落审计、dev key 主体的 `scopes` 对齐），两处变更在下文逐条列出。

#### 目标

让 CLI 与 Web 走同一条 API Key 校验路径，且审计覆盖对等。

#### 适用范围（实际实施）

| 文件 | 变更 |
| --- | --- |
| `application/api_key_auth.py` | **新增**：`validate_api_key` / `ApiKeyAuthResult` / `write_auth_audit` / `record_api_key_auth` |
| `interfaces/cli.py` | `_validate_api_key_for_cli` 改为委托并落审计；`_build_cli_principal` 与 `run_cli` 传入 `audit_store` |
| `interfaces/web/auth/deps.py` | `_validate_api_key` 只剩"从请求取上下文 + 把结果记进审计" |
| `interfaces/web/auth/audit.py` | `log_auth_event` 的写入委托给共用实现（"审计失败不致命 + 缺存储只告警一次"的策略只剩一份） |
| `tests/application/test_api_key_auth.py` | **新增**：共用规格（校验分支 / 审计写入 / 入口标记 / 结果不可变） |
| `tests/interfaces/test_cli.py` | 替身补 `audit_store` 字段；`scopes` 断言同步；新增 3 条 CLI 审计用例 |

#### 为什么放在 `application/`

校验 API Key 需要 `APIKeyRepository`（`application/ports.py` 已有该 Protocol）
与 `Principal`（`application/principal.py`）。放应用层符合既有分层，
且两端的审计差异正说明它属于"业务规则"而非"协议适配"。

#### 实施步骤（实际实施）

**步骤 1 —— 定义共享校验入口**：`validate_api_key(api_key, *, dev_key, store)` 返回
`ApiKeyAuthResult`——不只 `principal` 与 `reason`，而是**把该记的审计内容也算好**
（`event_type` / `actor_id` / `outcome` / `details`）。

> **与初版设计的差异**：初版只说"返回 `principal` 与 `reason`"。实际把审计**内容**
> 也放进结果，因为"事件类型与 actor 取什么值"本身就是同一条业务规则——
> 留给两个调用方各自拼，正是 `audit.py` docstring 里说的那种漂移。
> 审计**写入**仍在调用方（需要请求上下文），这条界线没变。

**步骤 2 —— Web 侧接入**：`deps._validate_api_key` 改为调用共享函数，再由
`record_api_key_auth` 落审计。**载荷逐字未变**：Web 侧不传 `entry`，
事件类型 / actor / action / details 与实施前一致。

**步骤 3 —— CLI 侧接入**：`_validate_api_key_for_cli` 保留为薄封装——CLI 的失败语义是
"抛异常让 `run_cli` 打印提示并退出 2"，而共用实现用 `principal=None` 表达失败。
审计就写在这一层：**失败路径上只有这里拿得到结果对象**，上层只看到一个异常，
写不出失败原因。`ip` / `user_agent` 传 `None` 而不是空串。

**步骤 4 —— 补齐 CLI 的审计写入**（**行为变更点①**）：

| 事件 | 含义 | 实测载荷 |
| --- | --- | --- |
| `apikey_auth_success` | CLI 取到凭据且校验通过 | `{"source": "env_dev_key", "entry": "cli"}` |
| `apikey_auth_failure` | 凭据无效 / 存储未装配 | `{"reason": "invalid_or_revoked", "entry": "cli"}` |

**步骤 5 —— `Principal` 字段对齐**（**行为变更点②**）：dev key 换来的主体，
CLI 侧此前不带 `scopes`、Web 侧带 admin 权限集；现在统一带。
代价是 `tests/interfaces/test_cli.py` 那条等值断言随之更新——**改的是期望，不是实现**。

#### 一处顺手修掉的缺陷

`secrets.compare_digest` 对非 ASCII 的 `str` 抛 `TypeError`，而 Web 侧的 `api_key`
来自请求头（不受信输入）——原实现会因此把认证路径变成 500，而正确结果是 401。
现在：`api_key` 非 ASCII 按"匹配不上"处理；`dev_key` 非 ASCII（运营方填错配置）
**留一条 WARNING**——静默跳过比对会让"应急入口怎么都进不去"变成一个查不出原因的现象。

#### 预期效果（实施后实测）

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| API Key 校验实现 | 2 份 | **1 份**（`application/api_key_auth.py`） |
| CLI 认证审计 | **0 条** | 成功 / 失败各 1 类，带 `entry="cli"` |
| dev key 主体的 `scopes` | 两端不同 | 两端一致 |
| "审计失败不致命 + 缺存储只告警一次"的策略 | 2 处（Web 一份，CLI 没有） | **1 处** |
| 换哈希算法 / 加缓存需要动的文件 | 2 个 | 1 个 |

#### 风险（实施后的实际结果）

| 风险 | 概率 | 结果 |
| --- | --- | --- |
| Web 侧行为被无意改变 | 中 | **未发生**：`tests/interfaces/web` 未改一行即通过；Web 侧不传 `entry`，载荷逐字保持 |
| CLI 新增审计导致既有断言失败 | 中 | 如预期发生 12 处：11 处是替身缺 `audit_store` 字段（替身没跟上 `AppContext` 的形状），1 处是 `scopes` 断言。均已同步 |
| CLI 无请求上下文导致审计字段缺失 | 中 | **未发生**：`audit_log` 的 `ip` / `user_agent` 本就可空，传 `None` |

#### 验收标准（已达成）

| 编号 | 标准 | 实测结果 |
| --- | --- | --- |
| 1 | 全仓只有一处 `compare_digest(api_key, ...)` 比对 | 已达成：只在 `application/api_key_auth.py` |
| 2 | Web 认证审计事件集合与实施前一致 | 已达成：`tests/interfaces/web` 87 项未改一行即通过 |
| 3 | CLI 成功与失败各落一条审计 | **真机复现**（独立探针库）：`apikey_auth_failure` 与 `apikey_auth_success` 各一条，`entry="cli"`、`ip` / `user_agent` 为 `NULL` |
| 4 | 两端 dev key 得到的 `Principal` 字段一致 | 已达成：`tests/application/test_api_key_auth.py` 断言到 `scopes`；CLI 侧等值断言同步更新 |
| 5 | 全量回归 | **1292 passed / 0 failed**；`lint-imports` 6 kept / 0 broken（101 文件 / 306 依赖） |

> **初版标准 3 写的是 `source="cli"`，实际用了 `entry`**：`source` 这个键在 dev key 场景
> 已被占用（`{"source": "env_dev_key"}`），复用它会把那个含义覆盖掉，
> 于是"用了环境变量里的应急密钥"这条信息消失。

#### 遗留（不属于本方案）

新增模块在编辑器里会报 7 条基于类型检查的提示（`dict[str, Any]`、对已注释为 `str` 的
参数做 `isinstance` 校验、隐式字符串拼接）。这些构造与仓库既有代码一致
（`application/ports.py` 同样用 `dict[str, Any]` 承接 `details`；
`thread_utils.normalize_thread_id` 同样对 `str` 参数做类型校验），
且 CI 只跑 pytest 与 `lint-imports`、不跑类型检查。
要不要收紧是**全仓策略**问题，不在本方案里顺手改。

---

### 方案四：单文件规模收敛（对应 Q4）

> **实施状态：已取消（2026-09-20，用户决定）。** 本轮不做任何拆分，也不给文件头补理由。
> 下面保留的是**判据与逐文件处理方式**——"超标的文件里是否存在两段可各自独立变更的内容"——
> 将来若要做，可直接照用，不必重新分析。
>
> **已知代价（保留在案）**：这 7 个文件的规模此后不受任何约束，也不会有人被提醒。
> 已实测确认：本项目的 CI（`.github/workflows/ci.yml`）只跑 pytest 与 `lint-imports`，
> **没有任何行数检查**，所以"单文件超 500 行需给出理由"这一条**完全依赖人工**。
> Q4 因此转为已知限制，而不是已修复。

#### 目标

让每个超过 500 行的文件**要么拆开，要么在文件里写明理由**。
本条不追求"全部拆到 500 行以下"——`config.py` 的常量集中是有意为之。

#### 适用范围与处理方式

| 文件 | 行数 | 处理 |
| --- | --- | --- |
| `interfaces/web/static/app.js` | 2409 | **拆分**（见步骤 1） |
| `application/run_service.py` | 1342 | **拆分**（见步骤 2） |
| `config.py` | 1248 | **写明理由**（见步骤 3），暂不拆 |
| `runtime/thread_store.py` | 1103 | **写明理由**，暂不拆 |
| `application/thread_service.py` | 1069 | **写明理由**，暂不拆 |
| `runtime/knowledge_store.py` | 887 | **写明理由**，暂不拆 |
| `interfaces/web/routes.py` | 862 | **拆分**（见步骤 4） |

判据：行数超标的文件里，**是否存在两段可以各自独立变更的内容**。
有则拆，无则写明理由。

**步骤 1 —— `app.js` 拆分**：按"视图 / 状态 / 传输"三段切开。
前端无包管理（`static/` 目录下已是 `markdown.js`、`auth_credential.js` 两个独立文件，
说明本项目的前端约定就是"多文件 + `<script>` 引入"），
照既有约定拆为若干文件即可，不引入构建工具。

**步骤 2 —— `run_service.py` 拆分**：该文件在 2026-09-19 已拆过一次
（`run_registry` / `run_governance` / `run_branch`，见《运行服务模块拆分实施记录》），
但仍有多处"一行委托"样板。本次先列出可独立变更的候选段落（事件流编码、
中止/超时收口、检查点读写），**逐段核对变更理由是否唯一**，只拆真正独立的段。
若核对后判定"剩下的都是同一条流水线，拆开反而割裂"，则写明理由并收工。

**步骤 3 —— 写明理由的格式**：在被判定"暂不拆"的文件头部 docstring 增加一段：

```
WHY 本文件超过 500 行：<具体理由，例如"全部方法是同一张表的 CRUD，
变更理由唯一；拆开会让事务边界跨文件，反而更难看出锁的覆盖范围">
```

**步骤 4 —— `routes.py` 拆分**：把 7 个 DI 辅助（`:73-114`）与 SSE 编码（`:859`）
移出。两者与"REST 端点定义"的变更理由不同：DI 辅助随路由前缀与 state 结构变，
SSE 编码随事件协议变。移出后 `routes.py` 只剩端点。
**不拆端点**——21 个端点共享同一路由表与同一前缀，拆开会让"这个前缀下有哪些端点"
需要翻多个文件。

**步骤 5 —— 修正 `application/__init__.py` 的层声明**（Q4 的第二处）：
docstring 改为描述"本层包含哪些职责域"，或明确区分"对外导出的稳定契约（12 个符号）"
与"层内模块（29 个）"。当前写法会被读成"这一层只有三个东西"。

#### 预期效果

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| 超 500 行且无理由的文件 | 7 个 | 0 个 |
| `app.js` | 2409 行单文件 | 多个文件，每个 < 800 |
| `routes.py` | 862 行（端点 + DI + SSE） | 只含端点 |
| 层的对外声明 | 与事实不符 | 与事实一致 |

#### 风险

| 风险 | 概率 | 缓解 |
| --- | --- | --- |
| 前端拆分破坏 `<script>` 加载顺序 | 中 | 按"被依赖者先加载"排序；拆分后跑前端既有测试（`tests/frontend/`，4 个文件） |
| `run_service.py` 拆分引入运行期行为差异 | 中 | 该文件是运行主路径，拆分后必须跑 `scripts/smoke_stop_kill.py` 与 `tests/application/` 全量 |
| 为凑行数而拆分，割裂同一处逻辑 | 中 | 步骤 2 已明确"核对后判定不可拆则写明理由"，允许不拆 |

#### 验收标准

1. 全仓不存在"超 500 行且未在 docstring 写明理由"的源码文件
2. `pytest tests/` 全量通过
3. `python scripts/smoke_stop_kill.py` 通过（运行路径回归）
4. `application/__init__.py` docstring 与 `application/` 实际模块集合一致

---

### 方案五：`open_*` 样板收敛为公共 helper（对应 Q5）

> **实施状态：已完成（2026-09-20）**。前置的"异常归因"修复由提交 `fdcec20` 单独落地——
> 先提交它，是为了让本方案的 diff 只含"纯结构收敛"，而不是同时含"修一个真缺陷"与
> "重写七处函数体"（那样一旦出问题无法对分）。
> **四处与初版设计不同**，逐条见下「实施偏离」。

#### 目标

把 7 份逐字重复的连接生命周期收敛为 1 份实现，且**保留**既有契约测试所守的性质。

#### 适用范围（实际实施）

| 文件 | 变更 |
| --- | --- |
| `runtime/sqlite_lifecycle.py` | **新增**（126 行）：`open_sqlite_store(db_path, *, label, prepare, row_factory, autocommit)` |
| `runtime/{api_key,audit,skill,thread,usage,knowledge}_store.py` + `runtime/store.py` | 7 个入口改为调用它；每处只留一个 `_prepare_*` 回调 + 一行"已就绪"日志 |
| `tests/runtime/test_open_store_contract.py` | 纳入第 7 个入口 `open_store`；docstring 从"六份副本的共同约定"改写为"那份实现的行为规格" |

#### 实施偏离（四处，逐条给理由）

**偏离 1 —— 模块名用 `sqlite_lifecycle.py`，而不是初版写的 `sqlite_open.py`**。
既有 `runtime` 模块名都是名词短语（`interval_worker` / `execution_registry` / `host_shell`）；
且 `open_sqlite_store` 本身已经和公开的 `open_*_store` 家族接近，模块名说明"它管什么"
比说明"它做什么"更省事。

**偏离 2 —— 回调是 `prepare` 一个，而不是初版写的 `setup` + `build` 两个**。
知识库的初始化顺序是"先建表、再按建表结果决定产出什么对象"
（向量表有没有建成，决定 `KnowledgeStore` 的 `vector_enabled`）。拆成两个回调就必须在
它们之间传递中间状态，等于把一次初始化切开。一个回调既够用，也不逼调用方改结构。

**偏离 3 —— 关闭日志级别统一为 DEBUG**（初版没提；属实施中发现的既有不一致）。
原状是三种：`usage` / `thread` / `store` 记 INFO，`skill` / `knowledge` 记 DEBUG，
`api_key` / `audit` 一条都没有。统一到 DEBUG 的理由：它是诊断细节，而"已就绪"才是
启动横幅；7 个存储在退出时各留一条 INFO 会把退出日志占满。
**这是行为变更，不是静默的**：实测 `main.py cli` 的退出日志从多条"连接已关闭"
降到 1 条（只剩独立实现的 `checkpointer`）；需要时把日志级别调到 DEBUG 即可拿到。

**偏离 4 —— 顺手修掉一处静默吞错**：`thread_store` 的幂等迁移循环原本是
`except Exception: pass`，"真的写坏了（磁盘满、库损坏）"与"重复应用"被归成同一类且
完全无声。改为记 DEBUG 再跳过——控制流不变，只是不再无声。这条落在本方案的边界内
（那段代码本就要搬进 `prepare`），搬完只在原位置少一行、新位置多两行。

#### 实施步骤（实际实施）

**步骤 1 —— 提炼公共生命周期**：`open_sqlite_store` 固定承担七件事——校验 `db_path` 与
`label`、建父目录、建立连接（可选 `row_factory` / `autocommit`）、施加两个 PRAGMA、
在内层 `try` 里调用 `prepare` 并把失败记成"<label>初始化失败"、`yield`、`finally` 判空关闭
（DEBUG 日志）。

**步骤 2 —— 关键约束：`yield` 必须落在捕获初始化异常的 `try` 之外**。
这正是 `tests/runtime/test_open_store_contract.py` 记录的缺陷：`yield` 若在 `try` 里，
`async with` 主体抛出的异常会被误记成"初始化失败"，于是"真正原因只有一句话，
日志里却是 5 条初始化失败加 5 份重复堆栈"。收敛后这条约束**只需实现一次**，
且由既有测试继续守住——测试也从"守六份副本"变成"守那份实现"。

**步骤 3 —— 迁移分两批，而非初版写的"一次一个模块"**：先迁 3 个无差异点的
（`api_key` / `audit` / `skill`）验证 helper 本身，再迁 4 个带差异点的
（`thread` 的迁移循环、`usage` 的补列、`knowledge` 的向量扩展、`store` 的
`autocommit` + `row_factory=None`）。每批之间跑 runtime 全量 + 契约测试。
保留初版"不一次性全改"的意图，但把 7 个独立提交合并为 2 次验证——差异点集中在一处看，
比逐个来回切换更容易发现"某个入口其实并不满足公共签名"。

**步骤 4 —— 保留差异点**（与初版一致）：知识库的 `dims` / `model` / `vector_enabled`
与向量扩展加载、长期记忆的 `autocommit` / 不设 `row_factory`，都留在各自模块与
`prepare` 里。helper 只固化生命周期，不试图把它们的初始化**内容**也统一。

#### 预期效果（实施后实测）

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| 生命周期实现份数 | 7 | **1**（`sqlite_lifecycle.py`）+ 7 处调用 |
| "`yield` 必须在 try 外"的实现点 | 7 | **1** |
| 新增一个 store 需要的样板 | 约 40 行 | 约 10 行（一个 `_prepare_*` + 一行日志） |
| 契约测试覆盖的入口数 | 6 | **7**（补上 `open_store`，它此前是唯一不达标的那个） |
| 同一段 WHY 注释的副本数 | 6 | 1（搬进 helper） |

**规模账（如实记）**：7 个存储 + 契约测试 `237 insertions / 266 deletions` = 净 **-29 行**；
加上新增 helper 的 126 行，**总行数是净增的（约 +97）**。
收益不在行数，而在三件事：那段结构只剩一处（改一次就改完）；最易被重构改坏的性质
从注释变成断言；新增第 8 个存储的样板从约 40 行降到约 10 行。
口径与《运行服务模块拆分实施记录》一致——拆分/收敛从来不是为了让行数变小。

#### 风险（实施后的实际结果）

| 风险 | 概率 | 结果 |
| --- | --- | --- |
| 迁移中破坏"真故障仍要留记录"的性质 | 中 | **未发生**：契约测试 14 条断言（7 入口 × 2 方向）全过 |
| PRAGMA 差异被抹平 | 低 | **未发生**：7 个入口的 PRAGMA 集合本来就一致（WAL + busy_timeout），helper 无需参数化 |
| 单次大改导致定位困难 | 中 | 按偏离 3「步骤 3」分两批迁移，首批 3 个验证 helper 本身，第二批 4 个带差异点的 |
| 新增：关闭日志级别统一后的噪音变化 | 低 | 方向是**变安静**（部分 INFO → DEBUG）；已实测退出日志只剩 1 条，并记入偏离 3 |

#### 验收标准（已达成）

| 编号 | 标准 | 实测结果 |
| --- | --- | --- |
| 1 | `pytest tests/runtime/` 全量通过，含契约测试 | 374 passed；契约测试 **14 passed**（原 12 条 + 长期记忆 2 条） |
| 2 | 7 个 `open_*` 的公开签名与行为不变 | 既有测试一行未改即通过 |
| 3 | `runtime/` 内"初始化失败"的产生点只有 1 处 | 已达成：只在 `sqlite_lifecycle.py` 里 |
| 4 | `main.py cli` 在 apikey 且凭据无效时不再出现"初始化失败" | **已真机复现**：7 个存储全部"已就绪" → `错误：HARNESS_API_KEY 无效` → 知识库已关闭 → 核心依赖已释放，**0 条"初始化失败"** |
| 5 | 全量回归 | **1274 passed / 0 failed**；`lint-imports` 6 kept / 0 broken（100 文件 / 302 依赖） |

#### 实施前发现的现状（2026-09-20 核查，本方案的起因）

**已由 `fdcec20` 完成的部分——"异常归因"修复，但每个 store 各修一遍**：

六个 `open_*`（thread / audit / api_key / usage / skill / knowledge）的改动是同**一件事**：
把 `yield` 移出「捕获初始化异常」的 `try`，并把 `conn` 改为可空、`finally` 判空后关闭。
配套新增了 `tests/runtime/test_open_store_contract.py` 用两条断言把它钉住。

也就是说：**原始缺陷（主体异常被误记为"初始化失败"）已经修好**，
但修法是"同一处结构写六遍"——而现在它变成了**六份带注释的同一处结构**：

> 实测：`只把初始化包在捕获里` 这段 5 行 WHY 注释，逐字出现在
> `runtime/` 下 6 个文件里（`search_content` 命中 6 个文件）。

修一次缺陷、重复多一份——这是本方案最直接的论据，比 Q5 当初的记载更有力。

**因此方案五的核心部分一行未做**，且收敛后这 6 段注释只需存在一处——
现在确实只剩 1 处（搬进了 `sqlite_lifecycle.py`）。

**新发现 1：契约测试的覆盖边界正好漏掉唯一不达标的实现**。

`test_open_store_contract.py` 的 `_OPENERS` 列了 6 个 opener，**没有 `open_store`**
（长期记忆，`runtime/store.py:25`）。而它恰好是唯一不满足第二条断言
（真·初始化失败要留下"初始化失败"记录）的实现——实测：把库路径占成目录后，

```
raised: OperationalError: unable to open database file
（日志里没有任何"初始化失败"记录）
```

原因是它的 `aiosqlite.connect` 与两个 PRAGMA 都在任何 `except` 之外。
契约的覆盖边界与"唯一不达标的那个"重合了——无论当初是否刻意，
这正是 Q1 说的那类盲区：**看起来被覆盖了**。

处理方式二选一，**选定 (a) 并已执行**：收敛成公共 helper 后 `open_store` 自然满足该契约，
于是把它加进 `_OPENERS`——契约的覆盖数从 6 变为 7，那条"边界与不达标者重合"的缝从此闭合。
选 (a) 而不是 (b) 的理由：它是**唯一**在真故障时不留线索的存储，而"真故障必须留线索"
正是这份契约存在的全部意义。

**新发现 2：`open_store` 与其余 6 个的差异不止这一点**——它多一个
`isolation_level=None`（事务边界由 `AsyncSqliteStore` 自己管），
且初始化是 `await store.setup()` 而不是 `executescript(_SCHEMA)`。
已按此实施：第一处差异由 helper 的 `autocommit` 开关承载，第二处留在它的 `prepare` 里。

**顺序（已执行）**：先以 `fdcec20` 单独提交那批"异常归因"修复，再做本方案。理由：本方案要
重写这 6 个函数体，叠在未提交的修复上会让一次 diff 同时包含"修一个真缺陷"与"纯结构收敛"，
出问题时无法对分，评审也无法按性质分开看。现在基线干净，逐模块迁移的每一步都能单独回退。

---

### 方案六：README 与文档对齐（对应 Q6、Q7）

> **实施状态：已实施（2026-09-20）。范围从 README 扩到 `architecture.html`**——
> 实施中发现后者才是被其它文档指定为"当前结构以此为准"的那一份，且它自己也已脱钩
> （缺 `bootstrap/`、残留已删除的 `tool/`）。只看 README 会留下一个更权威的错误源头。

#### 目标

使对外文档的目录结构与分层描述，与代码、两条规则载体四者一致。

#### 适用范围（实际实施）

| 文件 | 变更 |
| --- | --- |
| `README.md` | 第八节整体重写；第 1 节对 `agent/` 的定位措辞改为与目录树一致 |
| `docs/overview/architecture.html` | 第 3 章目录树按实际文件重生成；页脚状态补注改为如实描述 |
| 代码 | 零改动 |

#### 实施步骤

**步骤 1 —— README 目录树补齐并重写**：加入 `bootstrap/`、`scripts/`、`tests/`、`docs/`
与 8 个根级模块；分层约定改为两条依赖方向，并明确 `interfaces` **不得**直接依赖 `runtime`；
根级模块改用表格给出"为什么在根级"的一句话理由——读者真正需要的是这个，不是文件名清单。

**步骤 2 —— README 指向规则载体**：说明规则是**可执行的**，分别由
`.importlinter`（6 条包层契约，`uv run lint-imports`）与
`tests/test_root_module_contract.py`（根级模块角色表，`uv run pytest`）承担；
写明"为什么分成两处"（import-linter 只接受包作为分析根）；末尾指向本文档。

**步骤 3 —— 架构总览的目录树重生成**（实施中发现）：
`docs/overview/architecture.html` 第 3 章的目录树是三份文档中最旧的一份——
缺 `bootstrap/` 与全部根级模块，且残留 `tool/`（已在 P7 删除）。
它不是 README 的重复，而是被《分层架构重构实施记录》指定为"当前结构以此为准"的那一份，
因此必须一起修：按实际文件重生成，并在树前加一条快照说明。

**步骤 4 —— 页脚承诺改为如实描述**（实施中发现）：该页脚原文承诺
"所有位置引用格式为 `文件:行号`，与源文件一致"。第 3 章「关键文件职责」表的行号
已普遍滞后（实测三例）：

| 表中标注 | 实测 | 偏移 |
| --- | --- | --- |
| `main() main.py:79` | `main.py:137` | +58 |
| `open_thread_store() :360` | `runtime/thread_store.py:1046` | +686 |
| `ThreadMetaStore :76` | `runtime/thread_store.py:321` | +245 |

因此把承诺改写为"**符号名有效，请按名检索、不要按行号跳转**"，
并把行号复核登记为方案七（不塞进本次：逐行核对是另一件事，且它需要每个符号单独取证）。

#### 预期效果（实施后实测）

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| README 中出现的顶层模块 | 8 项（缺 `bootstrap/` 与 6 个根级模块） | 全部 14 项 |
| README 分层描述与规则冲突处 | 1 处（`interfaces → … → runtime` 字面放行） | 0 处 |
| README 是否指出规则载体 | 否 | 2 处（表格 + 提示语） |
| `architecture.html` 目录树 | 缺 `bootstrap/` 与根级模块，含已删除的 `tool/` | 与实际文件一致 |
| 页脚"行号与源文件一致"的承诺 | 与事实不符 | 改为如实描述，并把复核登记为方案七 |

#### 风险

| 风险 | 概率 | 缓解 |
| --- | --- | --- |
| README 描述再次随时间漂移 | 高 | 关键变化：两个规则载体是**可执行的**，漂移会在 `lint-imports` / `pytest` 失败时暴露，而不是等读者发现。README 只写"入口与理由"，不复制依赖矩阵 |
| 改动 `architecture.html` 时误伤其它章节 | 低 | 只替换第 3 章的 `<pre>` 块与页脚状态注，其余章节未触碰 |

#### 验收标准（已达成）

核对口径（避免"看起来一致"）：目录树的每一项都必须在磁盘上存在；
`dir /b /a` 输出中的**全部顶层目录**与**全部根级 `.py` 模块**都必须出现在树中。
环境产物（`.git` / `.venv` / `.data` / `.idea` / `.pytest_cache` / `.import_linter_cache` /
`__pycache__`）与用户本地的 `.env` 不计入。

| 编号 | 标准 | 实测结果 |
| --- | --- | --- |
| 1 | 树中不存在磁盘上没有的条目 | README 27 项、`architecture.html` 27 项，幻觉条目 **0** |
| 2 | 顶层目录与根级模块全部被覆盖 | 两份文档均**未覆盖 0 项** |
| 3 | README 不再出现与规则冲突的分层描述 | 0 处（原 1 处已改） |
| 4 | 两份文档都指向规则载体 | README 指向 2 处；`architecture.html` 快照说明 1 处 |
| 5 | `architecture.html` 第 3 章树中不再出现 `tool/`，且含 `bootstrap/` | 已达成 |

---

### 方案七：复核 `architecture.html` 的位置引用（对应 Q7）

> **实施状态：已完成（2026-09-20）**。实际口径与初版有四处不同，见「实施偏离」。
> 关键数字：复核时清点到 **84 处**引用，其中**只有 3 处仍然正确**；
> 现全部复核完毕并补成带路径形式（共 102 处、涉及 22 个文件），
> 由 `tests/test_architecture_doc_contract.py` 持续守住。

#### 目标

把"所有位置引用与源文件一致"从**一句无法自证的承诺**，变成**可以复核的事实**。

#### 为什么与方案六分开

方案六按文档单元修（目录树、分层描述、页脚承诺），一次替换即完成；
方案七要逐符号取证（每个引用都要在源码里定位一次），是另一类工作。
两者混在一起会让方案六无法在小改动后收尾——这正是 Q6 当初积累成问题的路径。

#### 适用范围（实际实施）

| 文件 | 变更 |
| --- | --- |
| `docs/overview/architecture.html` | 全篇引用逐条复核：修正行号、把裸行号补成带路径形式、订正三处已失效的**内容**断言、订正 1 处引用文件就不对的 |
| `tests/test_architecture_doc_contract.py` | **新增**：把"引用与源码一致"变成 4 条断言 |
| 代码 | 零改动 |

#### 实施偏离（四处）

**偏离 1 —— 规模比预估大一个量级**：初版只点名第 3 章表的 13 行（作为"优先"那一批）。
实际清点是 **84 处**：**29 处带路径、55 处是裸行号**（`:343-346` 这种不写文件名的形式）。
裸行号是本次最费时的一类：它的归属只能靠读者从上下文推断，而同一个 `:151`
在 routes 表里指 `POST /runs`、在 app.js 表里指 `ensureThread`——全局替换必然串台。

**偏离 2 —— 修完之后把全部裸行号补成了带路径形式**（55 处）。理由不是整齐：
不带路径的行号**机器无从复核**。补完之后，全篇引用能被一条正则抽出来校验，
防线才可能写成 30 行，而不是一张近百行的映射表。

**偏离 3 —— 复核发现 4 处"内容已过时"，它们不是行号漂移**（见下表）。
这类问题比行号错更危险：读者会据此以为"某条违反架构的边是被接受的"。

**偏离 4 —— 防线做成"符号一致"而不是"行号在范围内"**。只检查行号没超出文件长度，
对 `main.py:79`（那一行实际是 `logging.basicConfig(...)`，文档说它是 `main()`）
这种典型漂移毫无察觉。实际断言是"引用紧邻的符号名必须出现在它指向的行或行区间里"，
另加"引用必须带路径"与"抽不到引用即失败"两条自检。

#### 复核结论（实测）

| 项 | 实测 |
| --- | --- |
| 复核时清点的引用 | **84 处**（29 带路径 + 55 裸行号） |
| 其中仍然正确的 | **3 处**（`pyproject.toml:4`、`runtime/checkpointer.py:19`、`agent/profiles.py:27` / `:43`） |
| 偏移幅度 | 60 ~ 700 行不等，且**偏移量不一致**——不能按常量批量修正 |
| 指向文件之外的 | **3 处**：`application/event_translator.py` 只有 284 行，引用却写 `:343-346` / `:359-362` / `:368-369`。那段代码在《运行服务模块拆分》时搬去了 `run_service.py`（现为 `:802` 与 `:894-896`） |
| 引用所在文件就不对的 | **1 处**：`ModelInfo` / `ThreadSummary` 被列在 `schemas.py` 下，实际定义在 `application/dto.py:40` / `:126`（`schemas.py:14` 只是再导出） |
| 现在（复核后） | **102 处**，全部带路径、涉及 22 个文件；其中 48 处能判定出紧邻符号，受"符号一致"断言约束 |

**4 处内容已过时**——复核前它们读起来像"被接受的架构事实"：

| 位置 | 原文字 | 实测 |
| --- | --- | --- |
| §2.1 依赖图 | 五层图，**完全没有 `bootstrap/`** | `bootstrap/` 是全应用唯一装配点，三个模块 |
| §2.2 表 + 警告框 | "依赖图上的唯一例外：`llm/registry.py` 反向导入了 `agent.profiles`" | `llm/` 对 `agent` 的导入**实测 0 处**，`.importlinter` 契约 2 为 KEPT。该例外已在 2026-09 重构中消除，文字留存至今 |
| §2.2 表 | `interfaces/*` 依赖 `runtime`；`runtime/*` 被 `interfaces` 依赖 | `interfaces/` 对 `runtime` 的直接导入**实测 0 处**（契约 1 为 KEPT）；`runtime` 由 `bootstrap` 装配后注入 |
| §6.2 档位表 | `sandbox` 档位"显式抛 `NotImplementedError`" | 该档位**已实现**：`build_sandbox_runner()`（`agent/backends.py:194`）+ `SandboxedFilesystemBackend`（`:195`），并打印"只做资源管控，不是安全边界"的告警（`:205-209`） |
| §3 表 | `routes.py`"13 个端点，2 个以 SSE 返回" | 实测 **19 个端点**、**4 处**以 `text/event-stream` 返回 |

#### 预期效果（实施后实测）

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| 仍然正确的引用 | 3 / 84 | 84 / 84（复核期），此后由测试守住 |
| 可机器校验的引用 | 0（裸行号无从判定归属） | **102**（全部带路径） |
| 引用的文件不对 | 1 处 | 0 |
| 内容已过时的断言 | 4 处 | 0（并在文中标注"原先写的是什么"，避免下次又被当成事实） |
| 行号再次漂移时 | 无人知道，直到有人跳错地方 | **CI 直接失败**，并指出是哪一处、那一行实际是什么 |

#### 风险（实施后的实际结果）

| 风险 | 概率 | 结果 |
| --- | --- | --- |
| 修正完成即开始第二次腐烂 | 高 | **已设防线**：4 条断言。注入验证：把 `main.py:137` 改成 `main.py:100` → 该文件失败并指出这处引用（撤回后恢复全绿） |
| 逐符号核对成本被低估 | 中 | **确实被低估**（估 13 处、实际 84 处）。成本主要花在"裸行号归属判定"与"区域型引用（如 `:205-209`）该锚到哪一行" |
| 符号一致性断言误报，防线被绕开或删掉 | 中 | 断言只对"紧邻符号是标识符形态"的引用生效；区域型引用与路由路径自动跳过。实施中它抓到 2 处真实不一致（`base_system_prompt` / `system_prompt_suffix`：行内实际是常量名 `_BASE_SYSTEM_PROMPT` / `_SYSTEM_PROMPT_SUFFIX`），改的是**文档措辞**，不是放松断言 |

#### 验收标准（已达成）

| 编号 | 标准 | 实测结果 |
| --- | --- | --- |
| 1 | 每个引用都能跳转到正确符号 | 已达成：84/84 复核期；注入验证表明漂移会被拦下 |
| 2 | 存在一条可执行检查，使文档不会无声腐烂 | 已达成：`tests/test_architecture_doc_contract.py` 4 项，含"抽不到引用即失败"的自检（防止防线整体空转） |
| 3 | 全量回归 | `uv run pytest` 全量通过；该文件 4 passed |

---

### 方案八：`application` 侧的运行时端口化（对应 Q8）

> **实施状态：已实施（2026-09-20）**。实测：`application` 内对 `runtime` **实现类**的
> 导入点由 5 个类降为 **0**（`ports.py` 新增 7 个协议 + 1 个组合协议）；
> 新增白名单契约 3 项；注入越界（`health.py` 的 `TYPE_CHECKING` 块里加回
> `runtime.thread_store.ThreadMetaStore`）时失败并指出 `application/health.py:25`，
> 撤回后恢复全绿；`tests/application` 既有用例**一行未改**即通过。

#### 目标

让"更换存储实现"的改动只落在 `bootstrap`：`application` 不再依赖 `runtime` 的**实现类**。
同时**明确划出不改的部分**——端口化的目的是"让实现可替换"，不是"给每个协作者画一个 Protocol"。

#### 判据（决定改什么、不改什么）

按"是否存在另一种实现"把 Q8 证据表切成四类：

| 类别 | 处置 | 理由 |
| --- | --- | --- |
| 有状态、变更理由随存储技术走：`ThreadMetaStore` / `AuditStore` / `UsageStore` / `KnowledgeStore` / `SkillStateStore` | **端口化** | 换 SQLite→其它 / 换向量后端时，`application` 不该动 |
| 无状态纯函数与上下文管理器：`workspace_files` / `tool_outputs` / `execution_registry` / `skill_view` / `skills` / `attachments` / `window_start` / `normalize_search_query` | **不端口化**，登记白名单 | 没有"另一种实现"的诉求；`workspace_files` 更是**路径校验安全边界的唯一实现**（各服务 docstring 反复强调"再写一遍就是给目录穿越留下第二个答案"），加 Protocol 只会诱导第二份实现——正是 Q2 的漂移形态 |
| 数据载体与异常：`ChunkInput` / `KnowledgeHit` / `EmbeddingError` | **保留直接依赖** | 它们的形状由两侧共同理解，与实现技术无关；捕获特定异常类型也必须拿到类 |
| `RateLimiter`（`RunRegistry` 自建） | **不端口化** | `run_registry.py:200-206` 已论证"限流器由登记表自己持有"（键是运行期才算出的主体），改为注入会与该理由冲突 |

> **技术约束（决定了"函数组不端口化"）**：`typing.Protocol` 的静态检查不支持**模块对象**
> 满足协议（模块的属性查找无法参与结构化子类型判定）。要让函数式存储端口化，
> 需先在 `runtime` 里包一个类——那属 `runtime` 侧改动，不在本方案范围。

#### 适用范围

| 文件 | 变更 |
| --- | --- |
| `application/ports.py` | **新增** 7 个 Protocol：`ThreadMetadataReader` / `ThreadMetadataWriter` / `ThreadBranchStore` / `AuditLog` / `UsageLedger` / `KnowledgeIndex` / `SkillState` |
| `application/{thread,run,usage,knowledge,skill}_service.py`、`run_branch.py`、`health.py`、`attachment_service.py` | 构造参数与内部注解改为 Protocol；删除对应 `runtime` 实现类的 import |
| `tests/application/test_runtime_port_contract.py` | **新增**：白名单契约（见步骤 3） |
| `runtime/**` | **零改动**（结构化子类型自动满足协议） |
| `bootstrap/**` | **零改动**（仍注入同一批实例） |

#### 实施步骤

**步骤 1 —— 按"能力"拆协议，不按类照抄**。方法清单以实测调用点为唯一来源，
签名照抄 `runtime` 对应方法（**不臆造**）：

| 新 Protocol | 承担的方法（实测调用点） |
| --- | --- |
| `ThreadMetadataReader` | `get`、`list_threads`、`count`、`ping` |
| `ThreadMetadataWriter` | `record_turn`、`touch`、`rename`、`set_archived`、`set_tags`、`delete` |
| `ThreadBranchStore` | `current_branch`、`list_branches`、`get_branch`、`set_branch_head`、`set_current_branch`、`upsert_branch` |
| `AuditLog` | `log`、`count_all`（`AuditSink` 保持不动，`interfaces` 继续用窄的那个） |
| `UsageLedger` | `record`、`summarize` |
| `KnowledgeIndex` | `vector_enabled` / `dims` / `model`（`@property`）+ `get_document`、`replace_document`、`delete_document`、`list_documents`、`stats`、`search_keyword`、`search_vector` |
| `SkillState` | `set_enabled`、`resolve` |

拆成三个 Thread 协议而不是一个，与 `audit_recorder.py` 的窄接口风格一致：
`attachment_service` 只读 `get`、`health` 只读 `ping`，它们不该被迫接受写入能力。

**步骤 2 —— 服务改标注**：只改**类型注解**，不改参数名与顺序。既有测试替身按 duck typing 提供方法，
因此不受影响（实测：替身只需满足方法集）。

**步骤 3 —— 白名单契约测试**：用 AST 扫描 `application/**` 的 import
（**复用 `tests/test_root_module_contract.py` 已有的扫描实现，不写第二份**——
这正是"规则单一来源"原则），白名单是**精确集合**而非上限：

```python
_ALLOWED_NON_PROTOCOL_IMPORTS: dict[str, frozenset[str]] = {
    "runtime.workspace_files": frozenset({"IMAGE_SUFFIXES", "WorkspacePathError", "list_directory",
                                          "looks_binary", "read_bytes_capped", "resolve_in_workspace",
                                          "to_virtual_path"}),
    "runtime.execution_registry": frozenset({"abort_scope", "bound_scope"}),
    "runtime.rate_limiter": frozenset({"RateLimiter"}),
    # ... 其余按 Q8 判据表登记
}
```

断言两条（与既有契约同构）：越界导入失败、**白名单过时条目也失败**。

**步骤 4 —— 分两批迁移**：先做无争议的 `AuditLog` + `UsageLedger`（方法少、签名已知），
跑一次全量；再做 `ThreadMetadata*` + `KnowledgeIndex` + `SkillState`。每批之间
`uv run pytest tests/application tests/runtime` + `uv run lint-imports`。

#### 预期效果

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| `application` 直依赖的 runtime 实现类 | 5 个类 + 1 个自建限流器 | 0 个（限流器在白名单内并注明理由） |
| 新增一种存储实现的波及面 | 改 `application` 的 import 与注解 | 只改 `bootstrap` |
| "哪些 runtime 依赖是有意保留的" | 无记录 | 白名单 + 每条的理由注释 |
| 后来者顺手端口化纯函数模块 | 可发生，无人拦 | 契约失败并指出"这类模块被有意排除" |

#### 风险

| 风险 | 概率 | 缓解 |
| --- | --- | --- |
| Protocol 漏声明方法 | 中 | **运行时不会报错**（实际传入的是真类），只有类型检查器能发现，而 CI 不跑 mypy。以 `runtime` 类方法为唯一来源逐条核对，并把这条风险的形态写进契约测试 docstring |
| 端口化被泛化到纯函数模块 | 中 | 白名单精确集合 + 双向断言；失败信息写明"这类模块被有意排除" |
| 既有测试替身因注解变化而失败 | 低 | 只改注解不改参数名/顺序；替身按 duck typing 提供方法 |

#### 验收标准

1. `application` 内 `from runtime.<实现模块> import <实现类>` 命中 **0**（白名单项除外）
2. 注入越界（如在 `health.py` 写 `from runtime.thread_store import ThreadMetaStore`）时契约失败并指出模块与符号；撤回后全绿
3. `uv run lint-imports` 仍 6 条全绿；`uv run pytest` 全量通过
4. `tests/application/**` 既有断言**一行未改**（本次不改行为，只改依赖表达）

---

### 方案九：`ToolBundle` 携带内置工具名（对应 Q9）

> **实施状态：已实施（2026-09-20）**。`ToolBundle.builtin_names` 落地；
> 两处**替身**随之补齐（`tests/application/test_run_tool_audit.py`、
> `tests/interfaces/web/test_tools_routes.py`）——它们的 bundle 未携带内置名，
> 于是内置工具被判成 `unknown`，正是"替身没跟上形状"这一类（与方案三的
> 11 处替身同类）。`application` 内 `from agent.tools import ...` 实测 **0 处**。

#### 目标

让"哪些工具是内置的"这一信息**随装配产物一起交出**，而不是让应用层回查内核常量。
同时把两条**经评估不改**的依赖记录在案，避免后来者重复评估。

#### 三条评估结论

| 依赖 | 结论 | 理由 |
| --- | --- | --- |
| `AgentRunContext`（`run_service.py:31`） | **不改** | `application` 是图的**调用方**，必须构造图的入参上下文——这是调用协议，不是实现细节。硬解会把"图的入参形状"复制成第二份定义（Q2 的教训） |
| `ANONYMOUS_USER_ID`（`principal.py:17` 等 3 处） | **不改**（理由入档） | `agent/run_context.py:29-38` 论证了它与记忆命名空间**同源**（用空串会让 `StoreBackend` 判为非法）；上提为根级中立模块会切断这层语义关联，而 `principal.py:13-16` 已把耦合显式注释在案。收益（少一条 import）小于语义损失 |
| `BUILTIN_TOOL_NAMES`（`tool_catalog.py:16`） | **改** | `application` 需要的是**装配结果**，而 `ToolBundle` 就是装配结果，却把"哪些是内置"留在 `agent.tools` 的常量表里 |

#### 适用范围

| 文件 | 变更 |
| --- | --- |
| `agent/tooling.py` | `ToolBundle` 增加字段 `builtin_names: frozenset[str] = frozenset()`；`build_tool_bundle` 填入 `BUILTIN_TOOL_NAMES` |
| `application/tool_catalog.py` | `_builtin_names()` 与 `source_of()` 改读 `self._bundle.builtin_names`；删除 `from agent.tools import BUILTIN_TOOL_NAMES` |
| `tests/application/test_tool_catalog.py` | 补一条断言：bundle 未提供 `builtin_names` 时全部落 `unknown` |

`ToolRegistry` 与 `agent.tools` 零改动——`BUILTIN_TOOL_NAMES` 仍是内核内唯一来源。

#### 预期效果

| 项 | 实施前 | 实施后 |
| --- | --- | --- |
| `application` 中 `from agent.tools import ...` | 1 处 | **0 处** |
| "内置"判定的来源 | 内核常量表 | 本次装配的结果（语义更准） |
| 评估过的"不改"依赖是否有记录 | 无 | 3 条判定在案 |

#### 验收标准

1. `application` 内 `from agent.tools import ...` 命中 **0**
2. 内置 / 自定义 / MCP 三类来源的展示与审计归因不变（`tests/application/test_tool_catalog.py` 全过）
3. `uv run pytest` 全量通过

---

### 方案十：修正应用层的对外声明（对应 Q10）

> **实施状态：已实施（2026-09-20）**。docstring 改为「职责域 + 对外契约」两段式，
> 逐一点名 29 个模块；步骤 2 的可选加固没有另写文件，并入方案十一的载体
> （`test_layer_purity_contract.py` 的两条断言：docstring 必须点名每个模块、
> `__all__` 的每个符号必须可导入）。本方案**未重启方案四的任何拆分**。

#### 目标

使 `application/__init__.py` 的 docstring 与层内实际模块集合一致——它是新读者理解这一层的唯一入口。

#### 实施步骤

**步骤 1 —— docstring 改为两段式**：先列职责域（模块级），再列对外导出的稳定契约（`__all__` 的 12 个符号）。
建议模板：

```text
应用层：把内核与基础设施的能力编排成稳定的对外业务契约。

职责域（层内模块，不被 __all__ 导出）：
- 会话与运行编排：thread_service / run_service / run_branch / run_governance / run_registry
- 领域能力服务：attachment_service / knowledge_service / memory_service / skill_service / usage_service / workspace_service / model_catalog / tool_catalog / health
- 对外契约：dto / errors / events / ports / principal / audit_recorder / ownership
- 协议翻译与支撑：event_translator / interrupt_codec / usage / runnable / thread_export / message_utils / audit_context / api_key_auth

对外导出的稳定契约只有 __all__ 中的 12 个符号；其余按具体模块路径导入。
（分组口径与方案十一的层内纯度契约一致，两者必须同时改。）
```

**步骤 2 —— 可选加固**：一条断言——`__all__` 的每个符号都能从本模块导入成功
（防"导出了不存在的名字"）。成本约 10 行。

#### 明确不做（理由入档）

- **不扩大 `__all__`**：把 9 个服务一并导出会把公共 API 面从 12 扩到 20+，而 `interfaces` 按具体模块导入并无问题（`interfaces/web/routes.py:18-47`）。最小导出面是正确的；
- **不拆子包**（如 `contracts/` + `services/`）：30 个文件规模下，导航收益小于改动成本（`interfaces` / `bootstrap` / `tests` 约百处 import 路径），且需同步两份文档与契约。若日后超过 45 个模块再议。

#### 验收标准

1. docstring 与 `application/` 实际模块集合一致（逐模块点名、无遗漏）
2. `uv run pytest` 全量通过

---

### 方案十一（可选加固）：包内依赖方向的契约（对应 Q11）

> **实施状态：已实施（2026-09-20）**。落地为 `tests/application/test_layer_purity_contract.py`
> 的 4 条断言（层内纯度 / 分组覆盖 / docstring 点名 / `__all__` 可导入）。
> 实施前核对：当前 **0 违规**，契约落地即绿。注入验证：在 `application/dto.py`
> 末尾加一处**函数内延迟导入** `from application.thread_service import ThreadService`
> 时失败并指出 `application/dto.py:424`，撤回后恢复全绿——该注入正是为了证明
> 静态扫描覆盖延迟导入这种历史藏身形式。
>
> 顺带一处收敛：`tests/test_root_module_contract.py` 的解析实现改为复用
> `tests/_ast_imports.py`（新契约也要用同一段"读源码 → 解析 → 失败即显式报错"），
> 角色表 10 项实测仍全过。

#### 目标

把"契约组保持纯净"从共识变成 CI 会拦的规则，补上 `.importlinter` 覆盖不到的包内盲区。

#### 分组与断言

| 组 | 模块 | 约束 |
| --- | --- | --- |
| 契约组 | `dto` / `errors` / `events` / `ports` / `principal` / `audit_recorder` / `ownership` | **不得** import 服务组模块（可互相依赖，可依赖中立模块与 `config`） |
| 服务组 | 其余 | 自由，但不得 import `interfaces` / `bootstrap`（已由契约 4 覆盖） |

判据：契约组的变更理由是"对外契约变化"，服务组的变更理由是"业务流程变化"。
后者依赖前者是正常的；**前者依赖后者**说明契约层被业务流程污染——那正是服务间互相 import 的开端。

载体：`tests/application/test_layer_purity_contract.py`，AST 扫描，与方案八的白名单契约同一手法。

#### 验收标准

1. 注入违规（在 `dto.py` 写 `from application.attachment_service import ...`）时契约失败并指出两端模块；撤回后全绿
2. 当前代码库实测 **0 违规**（实施前先核对，避免契约一落地就红）
3. `uv run pytest` 全量通过

---

## 5. 实施顺序与依赖

| 顺序 | 方案 | 对应问题 | 前置 | 风险 | 状态 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| **第一项** | 方案一（契约覆盖） | Q1 | 无 | 低 | **已完成**（2026-09-20） | **必须最先做**：它决定后续改动是否被持续守住 |
| **第二项** | 方案六（文档对齐） | Q6 / Q7 | 方案一 | 极低 | **已完成**（2026-09-20） | 实施中把 `architecture.html` 一并修掉——它是被指定为"当前结构以此为准"的那一份 |
| **第三项** | 方案二（规则去重） | Q2 | 无 | 低 | **已完成**（2026-09-20） | 一项偏离：截断需新增 `truncate_with_notice`，不能复用 `truncate`（长度口径相反） |
| **第四项** | 方案五（store 样板） | Q5 | 前置修复已提交（`fdcec20`） | 中 | **已完成**（2026-09-20） | 收敛为 `runtime/sqlite_lifecycle.py`；契约覆盖数 6 → 7（补上 `open_store`）；四处实施偏离见该节 |
| **第五项** | 方案三（认证收敛） | Q3 | 方案二 | 中 | **已完成**（2026-09-20） | 唯一有意改变行为的一项：CLI 补审计 + dev key 的 `scopes` 两端对齐；另修掉一处「非 ASCII 凭据把 401 变 500」 |
| — | 方案四（单文件规模） | Q4 | — | — | **已取消**（2026-09-20，用户决定） | 判据与逐文件处理方式保留在该节；Q4 转为**已知限制**（CI 无行数检查，全靠人工） |
| **第六项** | 方案七（引用复核） | Q7 | 方案六 | 低 | **已完成**（2026-09-20） | 84 处引用逐条复核（原只有 3 处正确）；另订正 4 处已失效的内容断言，并加防线 |

**为什么方案一必须最先做**：其余五项的实施都会改动模块依赖
（方案三新增 `application` 模块、方案四移动代码、方案五新增 `runtime` 模块）。
没有契约覆盖时，这些改动只能靠人肉复核；有契约覆盖后，越界会在提交时立刻暴露。

### 5.1 第二批次（application 层，2026-09-20 追加）

| 顺序 | 方案 | 对应问题 | 前置 | 风险 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **第一项** | 方案十（层声明修正） | Q10 | 无 | 极低 | **已完成**（2026-09-20） |
| **第二项** | 方案十一（层内纯度契约） | Q11 | 方案十（先有分组词汇） | 低 | **已完成**（2026-09-20） |
| **第三项** | 方案八（运行时端口化） | Q8 | 方案十一 | 中 | **已完成**（2026-09-20） |
| **第四项** | 方案九（`ToolBundle` 携带内置名） | Q9 | 无 | 低 | **已完成**（2026-09-20） |
| **第五项** | 本批次文档同步（`.importlinter` 指路注释、README、`architecture.html`） | Q8/Q9 | 方案八/九 | 低 | **已完成**（2026-09-20） |

排序理由：方案十定义层内"职责域"词汇，方案十一的分组正取自它；
方案十一先落地，方案八的大范围注解改动才有第一层护栏。
方案九与方案八无耦合，可独立实施。

#### 第二批次实施结果（2026-09-20 实测）

| 项 | 实测 |
| --- | --- |
| `application` 内对 `runtime` 实现类的导入点 | **0**（实施前 5 个类） |
| `application` 内 `from agent.tools import ...` | **0**（实施前 1 处） |
| `application/ports.py` | 新增 7 个协议 + 1 个组合协议（`ThreadMetadataStore`） |
| 新增契约测试 | 2 个文件 / 7 项断言（层内纯度 4 + 运行时端口 3） |
| 既有测试替身同步 | 3 处（两处 `ToolBundle` 替身、一处 `_bundle` 辅助） |
| 注入验证 | 层内纯度：`application/dto.py:424` 被拦；运行时端口：`application/health.py:25` 被拦。均已撤回 |
| `uv run lint-imports` | **6 kept / 0 broken**（101 文件 / 304 依赖） |
| `uv run pytest` | **1304 passed / 0 failed** |
| `python scripts/smoke_stop_kill.py` | **PASS**（取消后 1.8s 内整棵进程树被终止） |
| 行为变更 | **无**——`tests/application` 既有断言未改；改动集中在类型注解、`ToolBundle` 的一个新字段与测试替身 |

---

## 6. 总验收标准

### 6.1 架构约束（可自动校验）

| 编号 | 约束 | 校验方式 |
| --- | --- | --- |
| A1–A6 | 前序 6 条契约继续通过 | `uv run lint-imports` |
| A7 | 中立叶子模块（`config` / `text_utils` / `thread_utils` / `web_safety`）不依赖仓库内任何模块 | `tests/test_root_module_contract.py` 用例 1 |
| A8 | `knowledge_runtime` 不被业务层与其它插件直接依赖 | 同上，用例 4 |
| A9 | `knowledge_runtime` 不直接依赖 `agent` / `interfaces` / `bootstrap` | 同上，用例 1 |
| A10 | 工具插件不直接依赖 `application` / `runtime` / `interfaces` / `bootstrap` / `llm` | 同上，用例 1 |
| A11 | `main` 不直接依赖 `agent` / `runtime` / `llm` / `bootstrap` | 同上，用例 1 |
| A12 | 根目录不存在未声明角色的模块 | 同上，用例 3 |
| A13 | `application` 不依赖 `runtime` 的可替换实现类（白名单见方案八） | `tests/application/test_runtime_port_contract.py`（方案八） |
| A14 | `application` 的契约组模块不依赖服务组模块 | `tests/application/test_layer_purity_contract.py`（方案十一） |

### 6.2 结构指标（可人工核查）

| 编号 | 指标 | 目标值 |
| --- | --- | --- |
| S1 | 契约管不到的顶层模块数 | 0 |
| S2 | 会话 ID 规则的判断实现份数 | 1（已达成） |
| S3 | 截断判断逻辑份数 | 1（已达成，两个口径各一份；判断逻辑共用 `truncate_with_notice`） |
| S4 | API Key 校验实现份数 | 1（已达成） |
| S5 | 超 500 行且未写明理由的源码文件数 | 0 |
| S6 | `open_*` 生命周期实现份数 | 1（已达成） |
| S7 | README 与契约冲突处 | 0（已达成） |
| S8 | `architecture.html` 目录树与代码的差异项 | 0（已达成） |
| S9 | `architecture.html` 中已失效的行号引用 | 0（方案七） |
| S10 | `application` 内指向 `runtime` 实现类的导入点数 | 0（方案八） |
| S11 | `application` 内 `from agent.tools import ...` 的数量 | 0（方案九） |

### 6.3 功能回归清单

| 编号 | 场景 | 验证方式 |
| --- | --- | --- |
| F1 | `uv run lint-imports` | 全绿（6 条） |
| F2 | `uv run pytest` | 全量通过（含根级模块边界契约） |
| F3 | CLI 启动与一轮对话 | `python main.py cli` |
| F4 | Web 启动与一轮对话 | `python main.py web` |
| F5 | 认证：Web apikey 登录与失败 | 接口调用验证 |
| F6 | 认证：CLI apikey 成功与失败（含审计落库） | 查询审计表验证 |
| F7 | 联网工具（`web_tools`）经动态加载生效 | `python scripts/smoke_web_chain.py` |
| F8 | 知识库工具经动态加载生效 | `python scripts/smoke_knowledge.py` |
| F9 | 运行中止与超时路径 | `python scripts/smoke_stop_kill.py` |
| F10 | 根级模块边界契约（方案一的交付物） | `uv run pytest tests/test_root_module_contract.py`（10 项） |
| F11 | `application` 的运行时端口契约与层内纯度契约（方案八 / 十一） | `uv run pytest tests/application/test_runtime_port_contract.py tests/application/test_layer_purity_contract.py` |

---

## 7. 风险与缓解

| 风险 | 影响 | 概率 | 缓解措施 |
| --- | --- | --- | --- |
| 新增约束暴露既有违规，CI 立即变红 | 阻塞提交 | 中 | **已在方案一中兑现**：实施前逐模块核对依赖面（第 8 节附表），实测 8 个模块全部满足角色表；实施后 `1258 passed` + 6 条契约全绿 |
| 根级模块的规则与包层规则由两个机制承担，后人改错地方 | 浪费时间 | 中 | 双向交叉引用：`.importlinter` 尾部指向测试文件，测试文件 docstring 指向 `.importlinter` 与本文档 |
| 约束被"改走动态导入"绕过 | 规则失效 | 低 | 动态加载的**目标模块**已纳入守备范围（用例 1 覆盖插件本身）。残余风险：`importlib` 加载仓库外的模块，属部署行为，不在静态契约范围内 |
| 方案三改变 CLI 认证审计行为 | 审计数据口径变化 | 中 | 属预期变更，实施记录中显式标注；补 F6 回归 |
| 方案五迁移中破坏 `yield` 位置约束 | 误归因的故障排查成本回归 | 中 | 契约测试的 12 条断言专守两条性质；逐个模块迁移 |
| 方案四为凑行数而拆分，割裂同一处逻辑 | 可维护性下降 | 中 | 允许"写明理由不拆"；拆前先核对"是否存在两段可独立变更的内容" |
| 全部改完后 README 再次漂移 | 文档失去价值 | 高 | 方案六步骤 3：README 只写"入口与理由"，事实来源指向 `.importlinter` |
| 端口化被泛化到无状态模块，形成第二份路径校验 | 安全边界出现两个答案 | 中 | 方案八判据表明确排除，并由白名单契约双向断言锁死 |
| Protocol 漏声明方法——**运行时不会报错**，只有类型检查器可见，而 CI 不跑 mypy | 类型注解失去意义 | 中 | 方案八风险表已记录：以 `runtime` 类方法为唯一来源逐条核对；是否引入 mypy 属全仓策略，另议 |
| 层声明（方案十）再次与模块集合脱钩 | 新读者理解偏差 | 中 | 方案十一的分组取自该声明，脱钩时契约测试会先失败 |

---

## 8. 附录

### 8.1 根级模块实测依赖表（方案一实施前核对，实施后复核）

数据来源：用 `ast` 解析 8 个根级模块的全部 import 节点（含 `if TYPE_CHECKING:` 块与函数内延迟导入），
只保留仓库内顶层模块名。

| 模块 | 仓库内依赖（实测） | 角色 | 表中声明的允许集合 |
| --- | --- | --- | --- |
| `config.py` | 无 | 中立叶子 | 空集 |
| `text_utils.py` | 无 | 中立叶子 | 空集 |
| `thread_utils.py` | 无 | 中立叶子 | 空集 |
| `web_safety.py` | 无 | 中立叶子 | 空集 |
| `knowledge_runtime.py` | `application.knowledge_service`、`llm.embeddings`、`runtime.knowledge_store`、`config`(TYPE_CHECKING) | 服务句柄 | `application` / `llm` / `runtime` / `config` |
| `knowledge_tools.py` | `agent.tools`、`knowledge_runtime`、`text_utils`、`config`(TYPE_CHECKING) | 工具插件 | `agent` / `config` / `knowledge_runtime` / `text_utils` |
| `web_tools.py` | `agent.tools`、`web_safety`、`text_utils`、`config`(TYPE_CHECKING) | 工具插件 | `agent` / `config` / `text_utils` / `web_safety` |
| `main.py` | `application.audit_context`、`config`、`interfaces.cli`（延迟）、`interfaces.web.app`（延迟） | 入口分发 | `application` / `config` / `interfaces` |

**结论：8 个模块的实测依赖集合与角色表逐项相等——方案一无需先改任何源码即可落地，
落地后 `uv run pytest tests/test_root_module_contract.py` 10 项全过（实测）。**

> 这份表格的纪律与代码里的 `_BOUNDARIES` 相同：它是**规格**而不是快照。
> 任一侧发生变化（新增依赖或删除依赖），表格与代码应一起改。

### 8.2 文件规模实测（>500 行）

数据来源：遍历全仓 `.py`（排除 `.data/`、`.venv/`、`__pycache__/`）与 `.js` 后按行数排序。
下表为**方案五后复测**的值（`thread_store` 由 1103 降到 1083、`knowledge_store` 由 887 升到 900，
后者是收敛时补写的 WHY 与 `_prepare_*` 的签名开销）。

| 文件 | 行数 |
| --- | --- |
| `interfaces/web/static/app.js` | 2409 |
| `application/run_service.py` | 1342 |
| `config.py` | 1248 |
| `runtime/thread_store.py` | 1083 |
| `application/thread_service.py` | 1069 |
| `runtime/knowledge_store.py` | 900 |
| `interfaces/web/routes.py` | 862 |
| `tests/interfaces/test_cli.py` | 808 |
| `tests/test_web_tools.py` | 685 |
| `scripts/inspect_thread.py` | 662 |
| `runtime/sandbox/_winjob.py` | 611 |
| `runtime/sandbox/wsl_runner.py` | 571 |
| `application/knowledge_service.py` | 548 |
| `runtime/sandbox/docker_runner.py` | 533 |
| `scripts/smoke_stop_kill.py` | 532 |
| `application/run_registry.py` | 522 |

测试与 `scripts/` 下的文件不纳入方案四：前者按被测对象组织、拆分只会让"一个功能的
用例散到多处"，后者是一次性探针，其价值在于"能整段读下来"。

**第二批次后复测（2026-09-20）**：本次改动只增删类型注解与一个字段，因此上面这张表
只有三个数字变化——`application/run_service.py` 1342 → **1340**、
`application/thread_service.py` 1069 → **1066**、
`application/knowledge_service.py` 548 → **549**；复测中另有三个文件出现在清单里
（`tests/interfaces/test_cli.py` **900**、`scripts/probe_embedding.py` **520**、
`web_tools.py` **511**）。最大文件仍是 `interfaces/web/static/app.js`（2409 行）。
新增的 `application/ports.py`（约 400 行）与两个契约测试文件均未越过 500 行。

### 8.3 规则重复实测清单（方案二实施后复核）

| 规则 | 权威实现 | 调用点 | 是否为委托 |
| --- | --- | --- | --- |
| 会话 ID 校验 | `thread_utils.py:25-44` | `runtime/thread_store.py:339-346` | 是 |
| 会话 ID 校验 | `thread_utils.py:25-44` | `runtime/attachments.py:95-110` | 是 |
| 会话 ID 校验 | `thread_utils.py:25-44` | `runtime/usage_store.py:325` | **是（方案二改，原为复制）** |
| 文本截断（展示口径） | `text_utils.py:truncate` | `application/run_service.py`、`runtime/thread_store.py` 经 `build_title` | 是 |
| 文本截断（工具输出口径） | `text_utils.py:truncate_with_notice` | `web_tools.py:102` | **是（方案二改，原为复制）** |
| 文本截断（工具输出口径） | `text_utils.py:truncate_with_notice` | `knowledge_tools.py:55` | **是（方案二改，原为复制）** |
| API Key 校验 | **无权威实现** | `interfaces/cli.py:149-176`、`interfaces/web/auth/deps.py:77-143` | 两份平级实现（**待方案三**） |

### 8.4 `application` 侧依赖实测表（方案八 / 九实施前核对，实施后复核）

数据来源：逐行检索 `application/` 内对 `runtime` / `agent` / `llm` 的导入点，并记录落点类别。
"处置"列对应方案八的判据表。

| 被导入符号 | 落点类别 | 处置 |
| --- | --- | --- |
| `runtime.thread_store.ThreadMetaStore` | 有状态实现类 | 端口化（`ThreadMetadata*` / `ThreadBranchStore`） |
| `runtime.audit_store.AuditStore` | 有状态实现类 | 端口化（`AuditLog`） |
| `runtime.usage_store.UsageStore` | 有状态实现类 | 端口化（`UsageLedger`） |
| `runtime.knowledge_store.KnowledgeStore` | 有状态实现类 | 端口化（`KnowledgeIndex`） |
| `runtime.skill_store.SkillStateStore` | 有状态实现类 | 端口化（`SkillState`） |
| `runtime.rate_limiter.RateLimiter` | 有状态实现类（由 `RunRegistry` 自建） | **不端口化**（`run_registry.py:200-206` 的理由） |
| `runtime.thread_store.normalize_search_query` | 无状态函数 | 不端口化（白名单） |
| `runtime.usage_store.window_start` | 无状态函数 | 不端口化（白名单） |
| `runtime.workspace_files.*` | 无状态函数 + 安全边界唯一实现 | 不端口化（白名单） |
| `runtime.tool_outputs.*` | 无状态函数 | 不端口化（白名单） |
| `runtime.execution_registry.{abort_scope,bound_scope}` | 上下文管理器 | 不端口化（白名单） |
| `runtime.attachments.*` | 函数式存储（Protocol 无法约束模块对象） | 不端口化（白名单），并在契约测试 docstring 写明该限制 |
| `runtime.skill_view.*`、`runtime.skills.inspect_skills` | 无状态函数 | 不端口化（白名单） |
| `runtime.knowledge_store.{ChunkInput,KnowledgeHit}` | 数据载体 | 保留直接依赖 |
| `llm.embeddings.EmbeddingError` | 异常类型 | 保留直接依赖 |
| `agent.run_context.AgentRunContext` | 图调用协议 | **不改**（方案九评估） |
| `agent.run_context.ANONYMOUS_USER_ID` | 记忆命名空间兜底标识 | **不改**（方案九评估，理由入档） |
| `agent.tools.BUILTIN_TOOL_NAMES` | 内核常量表 | 改由 `ToolBundle.builtin_names` 提供（方案九） |

> 纪律与 8.1 相同：它是**规格**而不是快照。新增依赖或处置变化时，表格与代码一起改；
> 方案八的白名单契约测试是本表的可执行副本。
>
> **实施后复核（2026-09-20）**："端口化"一列的五行已全部落地，"不改"的三行保持原样，
> 白名单一列的图表已逐字落成 `tests/application/test_runtime_port_contract.py` 的
> `_ALLOWED_RUNTIME_IMPORTS`；实测应用层对实现类的导入为 0。

### 8.5 参考

- 前序文档：`分层架构评估与重构方案.md`、`分层架构重构实施记录.md`、`运行服务模块拆分实施记录.md`
- 当前结构总览：`docs/overview/architecture.html` 第 2、3 章
- import-linter 官方文档：<https://import-linter.readthedocs.io/>

---

*本文档为治理方案的设计依据。实施后的差异应同步更新至本文档，或另建《架构遗留问题治理实施记录》。*
